# TASK-0068: EditScreenViewModel suggestTags()実装 ✅ **完了** (TDD開発完了 - 7テストケース全通過)

**タスクID**: TASK-0068
**タスクタイプ**: TDD
**推定工数**: 6時間
**フェーズ**: Phase 5 - タグ提案（Should Have）
**信頼性レベル**: 🔵 *requirements.md REQ-103, REQ-301, REQ-302, REQ-406・architecture.md「EditScreenViewModel 設計変更」・dataflow.md「機能2: タグ提案」より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/llm-memo-rewrite/requirements.md) - REQ-103, REQ-301, REQ-302, REQ-406
- **設計文書**: [📐 architecture.md](../../design/llm-memo-rewrite/architecture.md)

## タスク概要

`EditScreenViewModel` に `suggestTags()` メソッドを追加する。`sourceContent`（本文リライトと同一の入力ソース）とアプリ内固定プロンプトを使ってLLMにタグ候補を生成させ、既存の `tagsText` に追加する（置換ではない）。ローディング状態・エラー処理は `rewriteBody()` と同様のパターンを踏襲する。

## 依存タスク

- **前提タスク**: [TASK-0063: EditScreenViewModel Hilt化・rewriteBody()実装](TASK-0063.md)
- **後続タスク**: [TASK-0069: EditScreen「タグを提案」ボタンUI追加](TASK-0069.md)

## 完了条件

- [x] `EditFormState` に `isSuggestingTags: Boolean = false` が追加されている
- [x] `suggestTags()` が `sourceContent` を入力にLLM呼び出しを行い、成功時は `tagsText` に生成結果を追加（既存タグを保持したまま、カンマ区切りで連結）する
- [x] 失敗時は `tagsText` を変更せず `errorEvents` にエラーを発行する
- [x] `sourceContent` が空文字であってもガードせず実行する（EDGE-101と同様の方針）
- [x] `mise exec -- ./gradlew test` が成功する

---

## 実装詳細

### 1. EditFormStateへのisSuggestingTags追加 🔵

**信頼性**: 🔵 *REQ-201パターン踏襲（`isRewritingBody` と同様の状態管理）*

```kotlin
data class EditFormState(
    val vault: String = "",
    val title: String = "",
    val body: String = "",
    val tagsText: String = "",
    val folder: String = "",
    val customFields: List<CustomFieldState> = emptyList(),
    val isRewritingBody: Boolean = false,
    val isSuggestingTags: Boolean = false,
    // ...
)
```

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/ui/EditFormState.kt`

### 2. suggestTags()メソッドの実装 🔵

**信頼性**: 🔵 *REQ-301, REQ-302, REQ-406・architecture.md「追加するメソッド」より*

```kotlin
fun suggestTags() {
    viewModelScope.launch {
        _formState.value = _formState.value.copy(isSuggestingTags = true)
        val settings = llmSettingsRepository.getSettings().first()
        when (
            val result = llmRewriteRepository.rewrite(
                settings,
                TAG_SUGGESTION_PROMPT,
                sourceContent,
            )
        ) {
            is LlmRewriteResult.Success -> {
                val current = _formState.value.tagsText
                val merged = if (current.isBlank()) result.text else "$current, ${result.text}"
                _formState.value = _formState.value.copy(tagsText = merged)
            }
            is LlmRewriteResult.Failure -> _errorEvents.emit(result.messageResId)
        }
        _formState.value = _formState.value.copy(isSuggestingTags = false)
    }
}
```

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/ui/EditScreenViewModel.kt`

### 3. アプリ内固定プロンプトの定義 🟡

**信頼性**: 🟡 *テンプレート単位のカスタムプロンプトは要件上明示されていないための設計判断。dataflow.md「機能2: タグ提案」・design-interview.md「残課題」参照*

`rewriteBody()` はテンプレート単位の `bodyLlmPrompt` を使うのに対し、`suggestTags()` はテンプレートに紐づかないアプリ内固定プロンプトを使用する。string resourceとして定義し、ViewModelから参照する。

```xml
<string name="llm_tag_suggestion_prompt">以下の文章から関連するタグを3〜5個、カンマ区切りで提案してください</string>
```

```kotlin
private val TAG_SUGGESTION_PROMPT: String
    get() = context.getString(R.string.llm_tag_suggestion_prompt)
```

**実装ファイル**: `app/src/main/res/values/strings.xml`, `app/src/main/java/com/den4dr/share2Obsidian/ui/EditScreenViewModel.kt`

### 4. 成功時のtagsTextマージ処理 🔵

**信頼性**: 🔵 *REQ-302（生成結果を既存のタグ入力欄に追加してもよい）より*

`current.isBlank()` の場合は生成結果のみを設定し、既存タグがある場合はカンマ区切りで連結する（置換ではなく追加）。

```kotlin
val merged = if (state.tagsText.isBlank()) result.text else "${state.tagsText}, ${result.text}"
```

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/ui/EditScreenViewModel.kt`

### 5. 失敗時のエラー通知 🔵

**信頼性**: 🔵 *architecture.md「追加するメソッド」・NFR-201より*

失敗時は `tagsText` を変更せず、`_errorEvents.emit(result.messageResId)` によりEditScreen側へToast表示用のイベントを発行する。

```kotlin
is LlmRewriteResult.Failure -> _errorEvents.emit(result.messageResId)
```

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/ui/EditScreenViewModel.kt`

### 6. sourceContentが空文字の場合の扱い 🔵

**信頼性**: 🔵 *EDGE-101（LLM入力ソースが空文字であってもボタンを非活性化してはならず、空文字のままLLM APIへ送信する）と同様の方針*

`rewriteBody()` が `bodyLlmPrompt` の有無のみをガードするのと同様、`suggestTags()` は `sourceContent` の空文字チェックを行わず、常にLLM呼び出しフローを実行する。

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/ui/EditScreenViewModel.kt`

---

## 単体テスト要件

既存 `rewriteBody()` のテストパターン（MockK + kotlinx-coroutines-test）を踏襲する。

### テストケース1: suggestTags()成功時に既存tagsTextに生成結果が追加されること 🔵

**信頼性**: 🔵 *REQ-302より*

**Given**: `formState.tagsText` が `"private, tag1"` に設定されており、`llmRewriteRepository.rewrite(...)` が `LlmRewriteResult.Success("tag2, tag3")` を返すようモック化されている
**When**: `viewModel.suggestTags()` を呼び出す
**Then**: `viewModel.formState.value.tagsText` が `"private, tag1, tag2, tag3"` になる（既存タグが保持され、置換されない）

### テストケース2: 既存tagsTextが空文字の場合は生成結果のみがtagsTextに設定されること 🔵

**信頼性**: 🔵 *完了条件より*

**Given**: `formState.tagsText` が `""` に設定されており、`llmRewriteRepository.rewrite(...)` が `LlmRewriteResult.Success("tag1, tag2")` を返すようモック化されている
**When**: `viewModel.suggestTags()` を呼び出す
**Then**: `viewModel.formState.value.tagsText` が `"tag1, tag2"` になる

### テストケース3: suggestTags()失敗時にtagsTextが変更されず、errorEventsが発行されること 🔵

**信頼性**: 🔵 *完了条件・EDGE-004より*

**Given**: `formState.tagsText` が `"private"` に設定されており、`llmRewriteRepository.rewrite(...)` が `LlmRewriteResult.Failure(messageResId = R.string.error_llm_network)` を返すようモック化されている
**When**: `viewModel.suggestTags()` を呼び出す
**Then**: `viewModel.formState.value.tagsText` は `"private"` のまま変更されず、`viewModel.errorEvents` から `R.string.error_llm_network` が発行される

### テストケース4: suggestTags()実行中はisSuggestingTagsがtrueになり、完了後falseに戻ること 🔵

**信頼性**: 🔵 *REQ-201パターン踏襲*

**Given**: `llmRewriteRepository.rewrite(...)` の応答が保留される（`CompletableDeferred` 等で制御）
**When**: `viewModel.suggestTags()` を呼び出す
**Then**: 呼び出し直後は `viewModel.formState.value.isSuggestingTags` が `true` になり、応答完了後は `false` に戻る

### テストケース5: sourceContentが空文字の状態でもsuggestTags()がガードされず実行されること 🔵

**信頼性**: 🔵 *EDGE-101と同様の方針*

**Given**: `viewModel.initialize(...)` の `sourceContent` に `""` を渡して初期化されており、`llmRewriteRepository.rewrite(...)` がモック化されている
**When**: `viewModel.suggestTags()` を呼び出す
**Then**: `llmRewriteRepository.rewrite(settings, TAG_SUGGESTION_PROMPT, "")` が1回呼ばれる（空文字であっても呼び出しがスキップされない）

---

## 実装手順

### TDDタスクの場合
1. `/tsumiki:tdd-requirements TASK-0068` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 信頼性レベルサマリー

### 項目別信頼性

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 5 | 1 | 0 | 6 |
| 単体テスト | 5 | 0 | 0 | 5 |
| UI/UX要件 | 0 | 0 | 0 | 0 |

### 全体評価

- **総項目数**: 11項目
- 🔵 **青信号**: 10項目 (91%)
- 🟡 **黄信号**: 1項目 (9%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質
