# TASK-0070: CustomFieldState拡張・TemplateApplicator.buildCustomFields()のLLM対応

**タスクID**: TASK-0070
**タスクタイプ**: TDD
**推定工数**: 6時間
**フェーズ**: Phase 6 - カスタムフィールドのLLM生成（Could Have）
**信頼性レベル**: 🟡 *interfaces.kt「CustomFieldState（変更後）」・architecture.md「変更が必要な既存コンポーネント」より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/llm-memo-rewrite/requirements.md) - REQ-104, REQ-304
- **設計文書**: [📐 architecture.md](../../design/llm-memo-rewrite/architecture.md)

## タスク概要

`CustomFieldState`に`valueSource`と`llmPrompt`を追加し、`TemplateApplicator.buildCustomFields()`が`FieldValueSource.LLM`のフィールドを空文字で初期化するよう対応する。実際のLLM生成はテンプレート適用時ではなく、EditScreen上のボタン押下時に行う（TASK-0072で実装）。

## 依存タスク

- **前提タスク**: [TASK-0056: Template/TemplateField/FieldValueSource ドメインモデル変更](TASK-0056.md), [TASK-0057: TemplateEntity/TemplateFieldEntity/AppDatabase Migration(2→3)・Repositoryマッピング修正](TASK-0057.md)
- **後続タスク**: TASK-0072

## 完了条件 ✅ **完了** (TDD開発完了 - 8テストケース全通過)

- [x] `CustomFieldState`に`valueSource: FieldValueSource = FieldValueSource.FIXED`, `llmPrompt: String = ""`が追加される
- [x] `buildCustomFields()`が`FieldValueSource.LLM`の場合、値を空文字、`valueSource`と`llmPrompt`（テンプレートの`TemplateField.llmPrompt`）を正しく設定した`CustomFieldState`を生成する
- [x] 既存の`FIXED`/`HTML_META`/`URL`/`EMPTY`の挙動が変更されない（回帰なし）
- [x] 既存の単体テストがすべて通る

---

## 実装詳細

### 1. `CustomFieldState`への`valueSource`/`llmPrompt`追加 🟡

**信頼性**: 🟡 *EditScreen側でLLM生成ボタン表示判定に使用するための設計上の拡張。直接ヒアリングはしていないが要件から論理的に導出される。interfaces.kt参照*

```kotlin
data class CustomFieldState(
    val key: String,
    val value: String,
    val valueType: FieldValueType,
    val valueSource: FieldValueSource = FieldValueSource.FIXED,
    val llmPrompt: String = "",
)
```

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/domain/model/CustomFieldState.kt`

### 2. `TemplateApplicator.buildCustomFields()`のLLM対応 🟡

**信頼性**: 🟡 REQ-304（生成タイミングをEditScreen上のボタン押下時に統一する設計判断。architecture.md「変更が必要な既存コンポーネント」参照）

```kotlin
fun buildCustomFields(template: Template?, processed: ProcessedContent): List<CustomFieldState> = template?.fields?.map { field ->
    val value = when (field.valueSource) {
        FieldValueSource.FIXED -> field.defaultValue
        FieldValueSource.HTML_META -> processed.metadata[field.metaKey] ?: ""
        FieldValueSource.URL -> processed.sourceUrl ?: ""
        FieldValueSource.EMPTY -> ""
        FieldValueSource.LLM -> ""
    }
    CustomFieldState(field.key, value, field.valueType, field.valueSource, field.llmPrompt)
} ?: emptyList()
```

`FieldValueSource.LLM`の場合、値は空文字で初期化し、実際の生成はEditScreen上の「生成」ボタン押下時（TASK-0072の`generateCustomFieldValue()`）に行う。

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/TemplateApplicator.kt`

---

## 単体テスト要件

### テストケース1: FieldValueSource.LLMのフィールドが空文字・正しいvalueSource・llmPromptで生成されること 🔵

**信頼性**: 🔵 *完了条件・REQ-304より*

**Given**: `valueSource = FieldValueSource.LLM`, `llmPrompt = "要約を作成してください"`の`TemplateField`を含む`Template`
**When**: `TemplateApplicator.buildCustomFields(template, processed)`を呼び出す
**Then**: 該当フィールドの`CustomFieldState`が`value = ""`, `valueSource = FieldValueSource.LLM`, `llmPrompt = "要約を作成してください"`を持つ

### テストケース2: 既存のFIXED/HTML_META/URL/EMPTYの挙動が変更されないこと（回帰確認） 🔵

**信頼性**: 🔵 *既存実装・完了条件より*

**Given**: `valueSource`が`FIXED`/`HTML_META`/`URL`/`EMPTY`である各`TemplateField`を含む`Template`と、対応する`ProcessedContent`
**When**: `TemplateApplicator.buildCustomFields(template, processed)`を呼び出す
**Then**: 各フィールドの`value`が既存実装と同じロジック（`defaultValue`/`metadata[metaKey]`/`sourceUrl`/空文字）で算出され、`valueSource`は各フィールドの`FieldValueSource`と一致し、`llmPrompt`は該当`TemplateField.llmPrompt`の値と一致する

### テストケース3: templateがnullの場合は空リストが返されること（回帰確認） 🔵

**信頼性**: 🔵 *既存実装より*

**Given**: `template = null`
**When**: `TemplateApplicator.buildCustomFields(null, processed)`を呼び出す
**Then**: 空リストが返される

---

## 実装手順

### TDDタスクの場合
1. `/tsumiki:tdd-requirements TASK-0070` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 信頼性レベルサマリー

### 項目別信頼性

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 0 | 2 | 0 | 2 |
| 単体テスト | 3 | 0 | 0 | 3 |
| 統合テスト | 0 | 0 | 0 | 0 |
| UI/UX要件 | 0 | 0 | 0 | 0 |

### 全体評価

- **総項目数**: 5項目
- 🔵 **青信号**: 3項目 (60%)
- 🟡 **黄信号**: 2項目 (40%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 要改善（設計上の拡張部分に🟡が残るが、既存実装調査・要件から妥当に導出されており実装着手は可能）
