# TASK-0058: LlmSettings・LlmSettingsRepository・LlmSettingsRepositoryImpl実装

**タスクID**: TASK-0058
**タスクタイプ**: TDD
**推定工数**: 8時間
**フェーズ**: Phase 1 - 基盤構築
**信頼性レベル**: 🔵 *architecture.md「LLM設定管理設計」・interfaces.kt「LLM 設定（新規）」より（一部 🟡 実装パターンの推測を含む）*

✅ **完了** (TDD開発完了 - 13テストケース全通過、TC-08は環境制約によりコンパイル成功で暫定確認)

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/llm-memo-rewrite/requirements.md) - REQ-004, REQ-401, NFR-101
- **設計文書**: [📐 architecture.md](../../design/llm-memo-rewrite/architecture.md)

## タスク概要

LLM API設定（`endpointUrl`/`apiKey`/`model`）を管理する新規Repositoryを実装する。`endpointUrl`/`model` は既存の `NoteSettingsRepositoryImpl`（DataStore Preferences、`app/src/main/java/com/den4dr/share2Obsidian/data/datastore/NoteSettingsRepositoryImpl.kt` 参照）と同様のパターンで実装し、`apiKey` のみ `EncryptedSharedPreferences` に分離保存する（REQ-401: 平文DataStoreへの保存は禁止）。

## 依存タスク

- **前提タスク**: [TASK-0055: 依存関係追加とプロジェクト設定](TASK-0055.md)
- **後続タスク**: TASK-0061, TASK-0066

## 完了条件

- [x] `LlmSettings(endpointUrl, apiKey, model)` data classが定義されている
- [x] `getSettings(): Flow<LlmSettings>` がDataStore（`endpointUrl`/`model`）とEncryptedSharedPreferences（`apiKey`）双方の変更を検知して発行する
- [x] `saveEndpointUrl()`, `saveApiKey()`, `saveModel()` がそれぞれ正しいストレージに保存する
- [x] `apiKey` が平文DataStoreに一切保存されないこと（REQ-401）

---

## 実装詳細

### 1. LlmSettings data class 🔵

**信頼性**: 🔵 *REQ-004, REQ-401・interfaces.ktより*

```kotlin
data class LlmSettings(
    val endpointUrl: String = "",
    val apiKey: String = "",
    val model: String = "",
)
```

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/data/llm/LlmSettings.kt`

### 2. LlmSettingsRepository インターフェース 🔵

**信頼性**: 🔵 *interfaces.kt「LlmSettingsRepository」より*

```kotlin
interface LlmSettingsRepository {
    fun getSettings(): Flow<LlmSettings>
    suspend fun saveEndpointUrl(url: String)
    suspend fun saveApiKey(apiKey: String)
    suspend fun saveModel(model: String)
}
```

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/data/llm/LlmSettingsRepository.kt`

### 3. LlmSettingsRepositoryImpl 🟡

**信頼性**: 🟡 *architecture.md「LLM設定管理設計」（実装パターンの詳細はヒアリング対象外の妥当な推測）より*

DataStore Preferences（`stringPreferencesKey("llm_endpoint_url")`, `stringPreferencesKey("llm_model")`）と `EncryptedSharedPreferences`（key: `"llm_api_key"`）を組み合わせて実装する。EncryptedSharedPreferencesの変更通知は `SharedPreferences.OnSharedPreferenceChangeListener` を `callbackFlow` でラップし、DataStoreの `Flow` と `combine()` する。

```kotlin
internal val ENDPOINT_URL_KEY = stringPreferencesKey("llm_endpoint_url")
internal val MODEL_KEY = stringPreferencesKey("llm_model")
internal const val API_KEY_KEY = "llm_api_key"

class LlmSettingsRepositoryImpl(
    private val dataStore: DataStore<Preferences>,
    private val encryptedPrefs: SharedPreferences,   // EncryptedSharedPreferences
) : LlmSettingsRepository {

    override fun getSettings(): Flow<LlmSettings> =
        dataStore.data.map { prefs ->
            (prefs[ENDPOINT_URL_KEY] ?: "") to (prefs[MODEL_KEY] ?: "")
        }.combine(encryptedApiKeyFlow()) { (endpointUrl, model), apiKey ->
            LlmSettings(endpointUrl = endpointUrl, apiKey = apiKey, model = model)
        }

    private fun encryptedApiKeyFlow(): Flow<String> = callbackFlow {
        trySend(encryptedPrefs.getString(API_KEY_KEY, "") ?: "")
        val listener = SharedPreferences.OnSharedPreferenceChangeListener { prefs, key ->
            if (key == API_KEY_KEY) trySend(prefs.getString(API_KEY_KEY, "") ?: "")
        }
        encryptedPrefs.registerOnSharedPreferenceChangeListener(listener)
        awaitClose { encryptedPrefs.unregisterOnSharedPreferenceChangeListener(listener) }
    }

    override suspend fun saveEndpointUrl(url: String) {
        dataStore.edit { it[ENDPOINT_URL_KEY] = url }
    }

    override suspend fun saveApiKey(apiKey: String) {
        withContext(Dispatchers.IO) {
            encryptedPrefs.edit().putString(API_KEY_KEY, apiKey).apply()
        }
    }

    override suspend fun saveModel(model: String) {
        dataStore.edit { it[MODEL_KEY] = model }
    }
}
```

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/data/llm/LlmSettingsRepositoryImpl.kt`

### 4. saveApiKey() の Dispatchers.IO 実行 🔵

**信頼性**: 🔵 *architecture.md「技術的制約」（EncryptedSharedPreferencesのAPIは同期的なため Dispatchers.IO への切り替えが必要）・AndroidX Security 公式パターンより*

`EncryptedSharedPreferences` は同期APIのため、`saveApiKey()` は `withContext(Dispatchers.IO)` 内で実行する。

---

## 単体テスト要件

### テストケース1: saveEndpointUrl/saveModel保存後にgetSettings()が更新値を返すこと 🔵

**信頼性**: 🔵 *既存 `NoteSettingsRepositoryImpl` の実DataStoreテストパターン踏襲（Robolectric使用）*

**Given**: 未初期化のDataStoreを持つ `LlmSettingsRepositoryImpl`
**When**: `saveEndpointUrl("https://api.example.com/v1/chat")` と `saveModel("gpt-4o-mini")` を呼び出す
**Then**: `getSettings().first()` が `endpointUrl = "https://api.example.com/v1/chat"`, `model = "gpt-4o-mini"` を含む `LlmSettings` を返す

### テストケース2: saveApiKey保存後にgetSettings()が更新値を返すこと 🔵

**信頼性**: 🔵 *architecture.md「LLM設定管理設計」より（MockKでSharedPreferencesをモック、またはRobolectricの実SharedPreferences使用）*

**Given**: 未初期化のEncryptedSharedPreferences相当のモック/実体を持つ `LlmSettingsRepositoryImpl`
**When**: `saveApiKey("sk-test-12345")` を呼び出す
**Then**: `getSettings().first()` が `apiKey = "sk-test-12345"` を含む `LlmSettings` を返す

### テストケース3: 初期状態（未保存）ではLlmSettings()のデフォルト値が返ること 🔵

**信頼性**: 🔵 *interfaces.kt デフォルト値定義より*

**Given**: 何も保存していない状態の `LlmSettingsRepositoryImpl`
**When**: `getSettings().first()` を呼び出す
**Then**: `LlmSettings(endpointUrl = "", apiKey = "", model = "")` と等しい値が返る

### テストケース4: DataStoreとEncryptedSharedPreferencesの変更が独立して反映されること 🟡

**信頼性**: 🟡 *`combine()` によるFlow合成の一般的な挙動からの妥当な推測*

**Given**: `endpointUrl`/`model` を保存済みの `LlmSettingsRepositoryImpl`
**When**: `saveApiKey()` のみを呼び出す
**Then**: `getSettings().first()` の `endpointUrl`/`model` は変更前の値を維持したまま、`apiKey` のみ更新される

---

## 統合テスト要件

### 統合テスト1: APIキーがSharedPreferencesファイルに平文で含まれないこと 🔵

**信頼性**: 🔵 *NFR-101・REQ-401より*

**テスト内容**: 実際の `EncryptedSharedPreferences.create()`（`MasterKey` + AES256_GCM/AES256_SIV）を使い `saveApiKey("sk-test-secret")` を実行後、当該SharedPreferencesファイルの実体（またはファイルI/O経由の内容）を検査する（androidTest）
**期待結果**: 保存したAPIキーの文字列 `"sk-test-secret"` がファイル内容に平文のまま含まれていない（暗号化されている）ことを確認する

---

## 実装手順

### TDDタスクの場合
1. `/tsumiki:tdd-requirements TASK-0058` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- `apiKey` を例外・ログに一切出力しないこと（NFR-102）
- `EncryptedSharedPreferences` の変更通知パターン（`callbackFlow` + `OnSharedPreferenceChangeListener`）は本プロジェクトでの実績がないため、`tdd-red`/`tdd-green` フェーズで動作検証を優先すること

---

## 信頼性レベルサマリー

### 項目別信頼性

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 3 | 1 | 0 | 4 |
| 単体テスト | 3 | 1 | 0 | 4 |
| 統合テスト | 1 | 0 | 0 | 1 |

### 全体評価

- **総項目数**: 9項目
- 🔵 **青信号**: 7項目 (78%)
- 🟡 **黄信号**: 2項目 (22%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質

**推測が多い項目**: EncryptedSharedPreferencesの変更通知をFlow化するパターン（`callbackFlow` + `OnSharedPreferenceChangeListener`）は、ヒアリング対象外の妥当な推測であり実装時に別パターン（都度読み取り等）への変更余地がある
