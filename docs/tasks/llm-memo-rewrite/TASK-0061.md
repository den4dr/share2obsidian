# TASK-0061: LlmModule Hilt DI設定 ✅ **完了**（ビルド・Dagger依存解決確認済み。実機起動確認は環境制約により未実施）

**タスクID**: TASK-0061
**タスクタイプ**: DIRECT
**推定工数**: 3時間
**フェーズ**: Phase 2 - LLM呼び出しロジック・DI設定
**信頼性レベル**: 🔵 *architecture.md「LLMリクエスト/レスポンス設計」「LLM設定管理設計」・Hilt公式パターンより*

### ステータス
- **状態**: ✅ 完了
- **完了日**: 2026-07-06
- **実装者**: Claude Code

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/llm-memo-rewrite/requirements.md) - REQ-401, REQ-402
- **設計文書**: [📐 architecture.md](../../design/llm-memo-rewrite/architecture.md)
- **外部API連携仕様**: [🔌 api-endpoints.md](../../design/llm-memo-rewrite/api-endpoints.md)

## タスク概要

`HttpClient`・`EncryptedSharedPreferences`・`LlmSettingsRepository`・`LlmRewriteRepository` を Hilt で Singleton 提供する新規 Hilt モジュール `LlmModule` を実装する。既存の `di/DataStoreModule.kt`, `di/DatabaseModule.kt` と同様のパターンを踏襲する。

## 依存タスク

- **前提タスク**: [TASK-0058: LlmSettingsRepositoryImpl実装](TASK-0058.md), [TASK-0060: LlmRewriteRepository・LlmRewriteRepositoryImpl実装](TASK-0060.md)
- **後続タスク**: TASK-0063, TASK-0066

## 完了条件

- [x] `@Module @InstallIn(SingletonComponent::class) object LlmModule` が実装されている
- [x] `HttpClient(CIO)` に `HttpTimeout`（30秒）と `ContentNegotiation`（json）がinstallされている
- [x] `EncryptedSharedPreferences` が `MasterKey`（AES256_GCM）で構築されている
- [x] `mise exec -- ./gradlew build` が成功し、アプリが起動できる（注: 実機/エミュレータでの起動確認は実行環境に`adb`・デバイスが存在せず未実施。代わりに一時`@EntryPoint`によるDagger依存解決の強制検証で確認。詳細は [verify-report.md](../../implements/llm-memo-rewrite/TASK-0061/verify-report.md) 参照）

---

## 実装詳細

### 1. provideHttpClient() 🔵

**信頼性**: 🔵 *architecture.md「LLMリクエスト/レスポンス設計」・「タイムアウト設定」より*

```kotlin
@Provides
@Singleton
fun provideHttpClient(): HttpClient = HttpClient(CIO) {
    install(ContentNegotiation) { json() }
    install(HttpTimeout) { requestTimeoutMillis = 30_000 }
}
```

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/di/LlmModule.kt`

### 2. provideEncryptedSharedPreferences() 🔵

**信頼性**: 🔵 *architecture.md「LLM設定管理設計」・AndroidX Security 公式パターンより*

```kotlin
@Provides
@Singleton
fun provideEncryptedSharedPreferences(
    @ApplicationContext context: Context
): SharedPreferences {
    val masterKey = MasterKey.Builder(context)
        .setKeyScheme(MasterKey.KeyScheme.AES256_GCM)
        .build()
    return EncryptedSharedPreferences.create(
        context,
        "llm_secure_prefs",
        masterKey,
        EncryptedSharedPreferences.PrefKeyEncryptionScheme.AES256_SIV,
        EncryptedSharedPreferences.PrefValueEncryptionScheme.AES256_GCM,
    )
}
```

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/di/LlmModule.kt`

### 3. provideLlmSettingsRepository() / provideLlmRewriteRepository() 🔵

**信頼性**: 🔵 *architecture.md「新規追加コンポーネント」表・既存 DataStoreModule.kt/DatabaseModule.kt のパターンより*

```kotlin
@Provides
@Singleton
fun provideLlmSettingsRepository(
    @ApplicationContext context: Context,
    encryptedPrefs: SharedPreferences,
): LlmSettingsRepository = LlmSettingsRepositoryImpl(context.llmSettingsDataStore, encryptedPrefs)

@Provides
@Singleton
fun provideLlmRewriteRepository(httpClient: HttpClient): LlmRewriteRepository =
    LlmRewriteRepositoryImpl(httpClient)
```

それぞれ `@Provides @Singleton` として提供する。既存の `di/DataStoreModule.kt`, `di/DatabaseModule.kt` と同様に `object LlmModule` 内の関数として定義する。

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/di/LlmModule.kt`

---

## 単体テスト要件

DIRECTタスクのため単体テストなし。`direct-verify` でビルド・起動確認のみ実施する。

---

## 統合テスト要件

### 統合テスト1: ビルド・起動確認 🔵

**信頼性**: 🔵 *完了条件「`mise exec -- ./gradlew build` が成功し、アプリが起動できる」より*

**テスト内容**: `mise exec -- ./gradlew build` を実行し、Hiltのコンポーネント生成（`LlmModule` の `@Provides` メソッドの依存解決）にエラーがないことを確認する。あわせてアプリを起動し、`EditScreenViewModel`（TASK-0063以降で `LlmRewriteRepository`/`LlmSettingsRepository` を注入予定）を含むHiltグラフ全体が構築できることを確認する
**期待結果**: BUILD SUCCESSFUL。アプリが `MainActivity` 起動時にクラッシュしない

---

## 実装手順

### DIRECTタスクの場合
1. `/tsumiki:direct-setup` - 直接実装・設定
2. `/tsumiki:direct-verify` - 動作確認・品質確認

---

## 信頼性レベルサマリー

### 項目別信頼性

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 3 | 0 | 0 | 3 |
| 単体テスト | 0 | 0 | 0 | 0 |
| 統合テスト | 1 | 0 | 0 | 1 |

### 全体評価

- **総項目数**: 4項目
- 🔵 **青信号**: 4項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質
