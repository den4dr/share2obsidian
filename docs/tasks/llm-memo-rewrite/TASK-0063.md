# TASK-0063: EditScreenViewModel Hilt化・rewriteBody()実装 ✅ **完了** (TDD開発完了 - 14テストケース全通過)

**タスクID**: TASK-0063
**タスクタイプ**: TDD
**推定工数**: 8時間
**フェーズ**: Phase 3 - 本文リライトUI（Must Have）
**信頼性レベル**: 🔵 *architecture.md「EditScreenViewModel 設計変更」・interfaces.kt「EditScreenViewModel変更後シグネチャ」より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/llm-memo-rewrite/requirements.md) - REQ-002, REQ-003, REQ-102, REQ-201, REQ-202, REQ-406, EDGE-101
- **設計文書**: [📐 architecture.md](../../design/llm-memo-rewrite/architecture.md)

## タスク概要

`EditScreenViewModel`を素の`ViewModel()`から`@HiltViewModel`化し、`LlmRewriteRepository`・`LlmSettingsRepository`をコンストラクタ注入する。`sourceContent`・`bodyLlmPrompt`をプライベート状態として保持し（`EditFormState`には含めない）、`rewriteBody()`メソッドと`errorEvents`（`SharedFlow<Int>`）を追加する。`rewriteBody()`はユーザーが編集した可能性のある`formState.body`ではなく、共有/取得直後の`sourceContent`を入力としてLLM呼び出しを行う。

## 依存タスク

- **前提タスク**: [TASK-0061: LlmModule Hilt DI設定](TASK-0061.md), [TASK-0062: MainActivity sourceContent退避・EditScreenViewModel.initialize()引数追加](TASK-0062.md)
- **後続タスク**: [TASK-0065: EditScreen UI「メモを更改」ボタン・ローディング・エラーToast実装](TASK-0065.md), TASK-0068, TASK-0072

## 完了条件

- [x] EditScreenViewModelが`@HiltViewModel @Inject constructor(llmRewriteRepository, llmSettingsRepository) : ViewModel()`になっている
- [x] initialize()の引数にsourceContent, bodyLlmPromptが追加され、プライベートプロパティとして保持される（EditFormStateには含まれない）
- [x] EditFormStateにisRewritingBody, rewriteBodyEnabledが追加され、rewriteBodyEnabledはinitialize()時に`bodyLlmPrompt.isNotBlank()`から算出される（REQ-102）
- [x] rewriteBody()がsourceContent（formState.bodyではない）を入力にLLM呼び出しを行い、成功時はformState.bodyを即時上書き、失敗時はerrorEvents(SharedFlow<Int>)にエラーメッセージのstring resource IDを発行する
- [x] sourceContentが空文字であってもrewriteBody()はガードせず実行する（EDGE-101）
- [x] MainActivityの`private val viewModel: EditScreenViewModel by viewModels()`はそのまま動作する（Hilt ViewModelもby viewModels()で取得可能）
- [x] 既存の単体テストがすべて通る

---

## 実装詳細

### 1. `@HiltViewModel`化とコンストラクタ注入 🔵

**信頼性**: 🔵 *architecture.md「design-interview.md Q2」より*

```kotlin
@HiltViewModel
class EditScreenViewModel @Inject constructor(
    private val llmRewriteRepository: LlmRewriteRepository,
    private val llmSettingsRepository: LlmSettingsRepository,
) : ViewModel() {
    // 既存の _formState, initialized 等はそのまま維持
}
```

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/ui/EditScreenViewModel.kt`

### 2. プライベート状態の追加 🔵

**信頼性**: 🔵 *REQ-406より*

```kotlin
private var sourceContent: String = ""
private var bodyLlmPrompt: String = ""
```

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/ui/EditScreenViewModel.kt`

### 3. `initialize()`への引数追加 🔵

**信頼性**: 🔵 *REQ-102より*

```kotlin
fun initialize(
    processed: ProcessedContent,
    config: NoteConfig,
    customFields: List<CustomFieldState>,
    sourceContent: String,
    bodyLlmPrompt: String,
) {
    if (initialized) return
    initialized = true
    this.sourceContent = sourceContent
    this.bodyLlmPrompt = bodyLlmPrompt
    _formState.value = EditFormState(
        // ...既存のプロパティ設定
        rewriteBodyEnabled = bodyLlmPrompt.isNotBlank(),
    )
}
```

`initialized`フラグによる画面回転時の重複初期化防止は既存実装のまま維持する。

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/ui/EditScreenViewModel.kt`

### 4. `errorEvents`（SharedFlow）の追加 🔵

**信頼性**: 🔵 *design-interview.md Q3より*

```kotlin
private val _errorEvents = MutableSharedFlow<Int>()
val errorEvents: SharedFlow<Int> = _errorEvents.asSharedFlow()
```

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/ui/EditScreenViewModel.kt`

### 5. `rewriteBody()`の実装 🔵

**信頼性**: 🔵 *REQ-002, REQ-003, REQ-201, REQ-406より*

```kotlin
fun rewriteBody() {
    viewModelScope.launch {
        _formState.update { it.copy(isRewritingBody = true) }
        val settings = llmSettingsRepository.getSettings().first()
        when (val result = llmRewriteRepository.rewrite(settings, bodyLlmPrompt, sourceContent)) {
            is LlmRewriteResult.Success ->
                _formState.update { it.copy(body = result.text) }
            is LlmRewriteResult.Failure ->
                _errorEvents.emit(result.messageResId)
        }
        _formState.update { it.copy(isRewritingBody = false) }
    }
}
```

呼び出しの入力は常に`sourceContent`であり、`formState.body`（ユーザーがEditScreen上で編集した可能性がある値）は使用しない。

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/ui/EditScreenViewModel.kt`

### 6. `sourceContent`が空文字の場合のガード省略 🔵

**信頼性**: 🔵 *EDGE-101より*

`rewriteBody()`は`sourceContent`の空文字チェックを行わず、常にそのままLLM呼び出しに渡す。ボタンの活性/非活性は`bodyLlmPrompt`の有無（`rewriteBodyEnabled`）のみで判定する。

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/ui/EditScreenViewModel.kt`

### 7. `EditFormState`への項目追加 🔵

**信頼性**: 🔵 *interfaces.kt「EditFormState（変更後）」より*

```kotlin
data class EditFormState(
    val vault: String,
    val folder: String,
    val title: String,
    val body: String,
    val tagsText: String,
    val customFields: List<CustomFieldState> = emptyList(),
    val isRewritingBody: Boolean = false,
    val rewriteBodyEnabled: Boolean = false,
)
```

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/ui/EditFormState.kt`

---

## 単体テスト要件（MockK + kotlinx-coroutines-test + Robolectricの既存パターンを踏襲）

### テストケース1: rewriteBody()成功時にformState.bodyがLLM応答テキストに更新されること 🔵

**信頼性**: 🔵 *REQ-003より*

**Given**: `llmRewriteRepository.rewrite(...)`が`LlmRewriteResult.Success("書き換え後テキスト")`を返すようMockKでスタブされている
**When**: `viewModel.rewriteBody()`を呼び出す
**Then**: `formState.value.body`が`"書き換え後テキスト"`に更新される

### テストケース2: rewriteBody()失敗時（各Failure種別）にformState.bodyが変更されず、errorEventsに対応するmessageResIdが発行されること 🔵

**信頼性**: 🔵 *EDGE-001〜004, NFR-201より*

**Given**: `llmRewriteRepository.rewrite(...)`が`LlmRewriteResult.Failure.NetworkError(R.string.error_llm_network)`等の各Failure種別を返すようスタブされている
**When**: `viewModel.rewriteBody()`を呼び出す
**Then**: `formState.value.body`は呼び出し前の値のまま変更されず、`viewModel.errorEvents`から対応する`messageResId`（例: `R.string.error_llm_network`）が1件発行される

### テストケース3: initialize()時、bodyLlmPromptが空文字の場合、formState.rewriteBodyEnabledがfalseになること（REQ-102） 🔵

**信頼性**: 🔵 *REQ-102より*

**Given**: `bodyLlmPrompt = ""`
**When**: `viewModel.initialize(processed, config, customFields, sourceContent = "本文", bodyLlmPrompt = "")`を呼び出す
**Then**: `formState.value.rewriteBodyEnabled`が`false`になる

### テストケース4: initialize()時、bodyLlmPromptが非空の場合、formState.rewriteBodyEnabledがtrueになること 🔵

**信頼性**: 🔵 *REQ-102より*

**Given**: `bodyLlmPrompt = "要約してください"`
**When**: `viewModel.initialize(processed, config, customFields, sourceContent = "本文", bodyLlmPrompt = "要約してください")`を呼び出す
**Then**: `formState.value.rewriteBodyEnabled`が`true`になる

### テストケース5: sourceContentが空文字の状態でrewriteBody()を呼んでも正常に処理が実行される（ガードされない）こと（EDGE-101） 🔵

**信頼性**: 🔵 *EDGE-101より*

**Given**: `initialize()`で`sourceContent = ""`を渡して初期化済み。`llmRewriteRepository.rewrite(...)`が呼ばれると`LlmRewriteResult.Success("結果")`を返すようスタブされている
**When**: `viewModel.rewriteBody()`を呼び出す
**Then**: `llmRewriteRepository.rewrite(settings, bodyLlmPrompt, "")`が実際に呼び出され（早期returnされない）、`formState.value.body`が`"結果"`に更新される

### テストケース6: rewriteBody()実行中はformState.isRewritingBodyがtrueになり、完了後falseに戻ること（REQ-201） 🔵

**信頼性**: 🔵 *REQ-201より*

**Given**: `llmRewriteRepository.rewrite(...)`の完了を制御可能な`CompletableDeferred`等でスタブする
**When**: `viewModel.rewriteBody()`を呼び出し、レスポンス完了前の状態を確認後、完了させる
**Then**: レスポンス完了前は`formState.value.isRewritingBody`が`true`であり、完了後は`false`に戻る

### テストケース7: rewriteBody()はsourceContentを入力とし、formState.body（ユーザーが編集した可能性がある値）を入力にしないこと（REQ-002, REQ-406の検証として重要） 🔵

**信頼性**: 🔵 *REQ-002, REQ-406より*

**Given**: `initialize()`で`sourceContent = "元コンテンツ"`を渡して初期化後、`viewModel.updateBody("ユーザーが編集した本文")`でformState.bodyを変更する
**When**: `viewModel.rewriteBody()`を呼び出す
**Then**: `llmRewriteRepository.rewrite(settings, bodyLlmPrompt, content)`が`content = "元コンテンツ"`で呼び出され、`"ユーザーが編集した本文"`では呼び出されない

---

## 実装手順

### TDDタスクの場合
1. `/tsumiki:tdd-requirements TASK-0063` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 信頼性レベルサマリー

### 項目別信頼性

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 7 | 0 | 0 | 7 |
| 単体テスト | 7 | 0 | 0 | 7 |

### 全体評価

- **総項目数**: 14項目
- 🔵 **青信号**: 14項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質
