# TASK-0066: SettingsViewModel LLM設定対応 ✅ **完了** (TDD開発完了 - 10テストケース全通過)

**タスクID**: TASK-0066
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 4 - LLM設定UI
**信頼性レベル**: 🔵 *requirements.md REQ-004・architecture.md「新規追加コンポーネント」表・interfaces.ktより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/llm-memo-rewrite/requirements.md) - REQ-004
- **設計文書**: [📐 architecture.md](../../design/llm-memo-rewrite/architecture.md)

## タスク概要

`SettingsViewModel` に `LlmSettingsRepository` を追加注入し、`SettingsUiState` に `llmEndpointUrl`/`llmApiKey`/`llmModel` を追加する。既存の `noteSettingsRepository.getSettings()` に加えて `llmSettingsRepository.getSettings()` を `combine()` し、両Repositoryの値を反映した単一の `uiState` を構築する。あわせて各値を保存する `updateLlmEndpointUrl()`/`updateLlmApiKey()`/`updateLlmModel()` を実装する。

## 依存タスク

- **前提タスク**: [TASK-0058: LlmSettingsRepositoryImpl実装](TASK-0058.md), [TASK-0061: LlmModule Hilt DI設定](TASK-0061.md)
- **後続タスク**: [TASK-0067: SettingsScreen LLM設定入力欄追加](TASK-0067.md)

## 完了条件

- [x] `SettingsViewModel` のコンストラクタに `private val llmSettingsRepository: LlmSettingsRepository` が追加されている
- [x] `SettingsUiState` に `llmEndpointUrl: String = ""`, `llmApiKey: String = ""`, `llmModel: String = ""` が追加されている
- [x] `uiState` が `noteSettingsRepository.getSettings()` と `llmSettingsRepository.getSettings()` を `combine()` して構築されている
- [x] `updateLlmEndpointUrl()`, `updateLlmApiKey()`, `updateLlmModel()` がそれぞれ対応するRepositoryのsave系関数を呼び出す
- [x] `mise exec -- ./gradlew test` が成功する

---

## 実装詳細

### 1. SettingsUiStateへのLLM設定フィールド追加 🔵

**信頼性**: 🔵 *REQ-004・interfaces.ktより*

```kotlin
data class SettingsUiState(
    val vault: String = "",
    val folder: String = "",
    val llmEndpointUrl: String = "",
    val llmApiKey: String = "",
    val llmModel: String = "",
)
```

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/ui/SettingsViewModel.kt`

### 2. LlmSettingsRepositoryのコンストラクタ注入 🔵

**信頼性**: 🔵 *architecture.md「新規追加コンポーネント」表より*

```kotlin
@HiltViewModel
class SettingsViewModel @Inject constructor(
    private val noteSettingsRepository: NoteSettingsRepository,
    private val llmSettingsRepository: LlmSettingsRepository,
) : ViewModel() {
    // ...
}
```

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/ui/SettingsViewModel.kt`

### 3. uiStateのcombine()化 🔵

**信頼性**: 🔵 *既存 `getSettings().map { ... }.stateIn(...)` パターンを踏襲した拡張*

```kotlin
val uiState: StateFlow<SettingsUiState> = combine(
    noteSettingsRepository.getSettings(),
    llmSettingsRepository.getSettings(),
) { note, llm ->
    SettingsUiState(
        vault = note.vault,
        folder = note.folder,
        llmEndpointUrl = llm.endpointUrl,
        llmApiKey = llm.apiKey,
        llmModel = llm.model,
    )
}.stateIn(viewModelScope, SharingStarted.WhileSubscribed(5_000), SettingsUiState())
```

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/ui/SettingsViewModel.kt`

### 4. update系関数の追加 🔵

**信頼性**: 🔵 *REQ-004・既存 `updateVault()`/`updateFolder()` パターンを踏襲*

```kotlin
fun updateLlmEndpointUrl(url: String) {
    viewModelScope.launch(Dispatchers.IO) { llmSettingsRepository.saveEndpointUrl(url) }
}

fun updateLlmApiKey(apiKey: String) {
    viewModelScope.launch(Dispatchers.IO) { llmSettingsRepository.saveApiKey(apiKey) }
}

fun updateLlmModel(model: String) {
    viewModelScope.launch(Dispatchers.IO) { llmSettingsRepository.saveModel(model) }
}
```

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/ui/SettingsViewModel.kt`

---

## 単体テスト要件

既存 `SettingsViewModelTest.kt` のパターン（MockK + kotlinx-coroutines-test）を踏襲する。

### テストケース1: updateLlmEndpointUrl()がsaveEndpointUrl()を呼び出すこと 🔵

**信頼性**: 🔵 *完了条件・既存 `updateVault()` テストパターンより*

**Given**: `llmSettingsRepository` がモック化されている
**When**: `viewModel.updateLlmEndpointUrl("https://example.com/v1/chat/completions")` を呼び出す
**Then**: `llmSettingsRepository.saveEndpointUrl("https://example.com/v1/chat/completions")` が1回呼ばれる

### テストケース2: updateLlmApiKey()がsaveApiKey()を呼び出すこと 🔵

**信頼性**: 🔵 *完了条件より*

**Given**: `llmSettingsRepository` がモック化されている
**When**: `viewModel.updateLlmApiKey("sk-xxxx")` を呼び出す
**Then**: `llmSettingsRepository.saveApiKey("sk-xxxx")` が1回呼ばれる

### テストケース3: updateLlmModel()がsaveModel()を呼び出すこと 🔵

**信頼性**: 🔵 *完了条件より*

**Given**: `llmSettingsRepository` がモック化されている
**When**: `viewModel.updateLlmModel("gpt-4o-mini")` を呼び出す
**Then**: `llmSettingsRepository.saveModel("gpt-4o-mini")` が1回呼ばれる

### テストケース4: uiStateがnoteSettingsRepositoryとllmSettingsRepository両方の値を反映して構築されること 🔵

**信頼性**: 🔵 *完了条件「uiStateはcombine()して構築される」より*

**Given**: `noteSettingsRepository.getSettings()` が `NoteSettings(vault = "MyVault", folder = "Inbox")` を、`llmSettingsRepository.getSettings()` が `LlmSettings(endpointUrl = "https://example.com", apiKey = "key", model = "gpt-4o")` をそれぞれ返す
**When**: `viewModel.uiState` を収集する
**Then**: `SettingsUiState(vault = "MyVault", folder = "Inbox", llmEndpointUrl = "https://example.com", llmApiKey = "key", llmModel = "gpt-4o")` と一致する

---

## 実装手順

### TDDタスクの場合
1. `/tsumiki:tdd-requirements TASK-0066` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 信頼性レベルサマリー

### 項目別信頼性

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 4 | 0 | 0 | 4 |
| 単体テスト | 4 | 0 | 0 | 4 |
| UI/UX要件 | 0 | 0 | 0 | 0 |

### 全体評価

- **総項目数**: 8項目
- 🔵 **青信号**: 8項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質
