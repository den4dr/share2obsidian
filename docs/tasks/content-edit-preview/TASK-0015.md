# TASK-0015: NoteConfig + NoteComposer 実装

**タスクID**: TASK-0015
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 1 - 基盤実装
**信頼性レベル**: 🔵 *要件定義書REQ-101, REQ-402・設計文書interfaces.ktより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/content-edit-preview/requirements.md) - REQ-101, REQ-103, REQ-402, REQ-405
- **設計文書**: [📐 architecture.md](../../design/content-edit-preview/architecture.md)
- **インターフェース定義**: [📝 interfaces.kt](../../design/content-edit-preview/interfaces.kt)
- **データフロー**: [🔄 dataflow.md](../../design/content-edit-preview/dataflow.md)

## タスク概要

`NoteConfig` データクラスと `NoteComposer` オブジェクトを新規実装する。
- `NoteConfig`: vault / folder / defaultTags を保持。`AppConfig` に依存せず明示的パラメータで動作する。将来のユーザー設定化への拡張ポイント。
- `NoteComposer`: 編集後の値から Frontmatter 文字列と Obsidian URI を生成する。既存の `FrontmatterBuilder` / `ObsidianUriBuilder` は変更しない（REQ-402）。

## 依存タスク

- **前提タスク**: なし
- **後続タスク**: [TASK-0020: MainActivity フロー変更](TASK-0020.md)

## 完了条件 ✅ **完了** (TDD開発完了 - 15テストケース全通過)

- [x] `NoteConfig.kt` が `format/` パッケージに作成されている
- [x] `NoteComposer.kt` が `format/` パッケージに作成されている
- [x] `NoteComposerTest.kt` のすべてのテストがパスする
- [x] 既存の `FrontmatterBuilder.kt` / `ObsidianUriBuilder.kt` に変更がない
- [x] `./gradlew test` がエラーなく通過する

---

## 実装詳細

### 1. NoteConfig データクラス 🔵

**信頼性**: 🔵 *設計文書 interfaces.kt・ユーザヒアリング（将来のユーザー設定化）より*

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/format/NoteConfig.kt`

```kotlin
package com.den4dr.share2Obsidian.format

import com.den4dr.share2Obsidian.AppConfig

data class NoteConfig(
    val vault: String,
    val folder: String,
    val defaultTags: List<String>
) {
    companion object {
        fun fromAppConfig(): NoteConfig = NoteConfig(
            vault = AppConfig.OBSIDIAN_VAULT,
            folder = AppConfig.OBSIDIAN_FOLDER,
            defaultTags = AppConfig.OBSIDIAN_TAGS
        )
    }
}
```

### 2. NoteComposer オブジェクト 🔵

**信頼性**: 🔵 *設計文書 interfaces.kt・REQ-101, REQ-103・既存 FrontmatterBuilder / ObsidianUriBuilder 実装より*

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/format/NoteComposer.kt`

```kotlin
package com.den4dr.share2Obsidian.format

import android.net.Uri

object NoteComposer {

    fun buildFrontmatter(title: String?, body: String, tags: List<String>): String {
        val titleLine = title?.let { "title: \"$it\"\n" } ?: ""
        val tagsString = tags.joinToString(", ")
        return "---\n${titleLine}tags: [$tagsString]\n---\n\n$body"
    }

    fun buildUri(content: String, title: String?, config: NoteConfig): Uri {
        return Uri.parse("obsidian://new").buildUpon()
            .appendQueryParameter("content", content)
            .appendQueryParameter("title", title ?: "")
            .appendQueryParameter("vault", config.vault)
            .appendQueryParameter("folder", config.folder)
            .build()
    }
}
```

---

## 単体テスト要件

**テストファイル**: `app/src/test/java/com/den4dr/share2Obsidian/format/NoteComposerTest.kt`

### テストケース1: タイトルありのFrontmatter生成 🔵

**信頼性**: 🔵 *REQ-101・acceptance-criteria.md TC-101-01より*

**Given**: title="テスト", body="本文テスト", tags=["shared", "web"]
**When**: NoteComposer.buildFrontmatter(title, body, tags)
**Then**: "---\ntitle: \"テスト\"\ntags: [shared, web]\n---\n\n本文テスト"

### テストケース2: タイトルなしのFrontmatter生成 🔵

**信頼性**: 🔵 *EDGE-001・FrontmatterBuilderの既存動作より*

**Given**: title=null, body="本文", tags=["shared"]
**When**: NoteComposer.buildFrontmatter(null, body, tags)
**Then**: "---\ntags: [shared]\n---\n\n本文"（titleフィールドを省略）

### テストケース3: 空タグリストのFrontmatter生成 🔵

**信頼性**: 🔵 *EDGE-003・requirements.md より*

**Given**: title=null, body="本文", tags=[]
**When**: NoteComposer.buildFrontmatter(null, body, [])
**Then**: "---\ntags: []\n---\n\n本文"

### テストケース4: buildUri の構造検証 🔵

**信頼性**: 🔵 *REQ-101・ObsidianUriBuilder 既存実装より*

**Given**: content="frontmatter", title="タイトル", config=NoteConfig(vault="v", folder="f", defaultTags=[])
**When**: NoteComposer.buildUri(content, title, config)
**Then**: URI に "content", "title", "vault=v", "folder=f" が含まれ、scheme="obsidian", host="new" である

### テストケース5: NoteConfig.fromAppConfig() 🔵

**信頼性**: 🔵 *REQ-405・AppConfig の値より*

**Given**: AppConfig.OBSIDIAN_VAULT, OBSIDIAN_FOLDER, OBSIDIAN_TAGS
**When**: NoteConfig.fromAppConfig()
**Then**: config.vault==AppConfig.OBSIDIAN_VAULT, config.folder==AppConfig.OBSIDIAN_FOLDER, config.defaultTags==AppConfig.OBSIDIAN_TAGS

---

## 統合テスト要件

### NoteComposer が FrontmatterBuilder と同等の出力を生成する 🟡

**信頼性**: 🟡 *REQ-402・既存FrontmatterBuilder実装から妥当な推測*

**テスト内容**: title="テスト", body="本文", tags=AppConfig.OBSIDIAN_TAGS の場合、NoteComposer.buildFrontmatter() の出力が FrontmatterBuilder.build() の出力と同等であること
**期待結果**: 同一文字列またはセマンティクス的に等価な Frontmatter

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-0015` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- `FrontmatterBuilder.kt` と `ObsidianUriBuilder.kt` は変更しない（REQ-402）
- `NoteComposer` は `AppConfig` をインポートしない（明示的パラメータのみ）
- テストで `Uri` を扱うため Robolectric または `androidTest` が必要な場合は `androidTest` に移動を検討

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 2 | 0 | 0 | 2 |
| 単体テスト | 5 | 0 | 0 | 5 |
| 統合テスト | 0 | 1 | 0 | 1 |

- **総項目数**: 8項目
- 🔵 **青信号**: 7項目 (88%)
- 🟡 **黄信号**: 1項目 (12%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質
