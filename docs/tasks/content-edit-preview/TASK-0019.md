# TASK-0019: EditScreen Composable 実装 ✅ **完了** (TDD開発完了 - 14テストケース全通過)

**タスクID**: TASK-0019
**タスクタイプ**: TDD
**推定工数**: 6時間
**フェーズ**: Phase 2 - UI・統合実装
**信頼性レベル**: 🔵 *REQ-002, REQ-003, REQ-004・ユーザヒアリング・NFR-101, NFR-102より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/content-edit-preview/requirements.md) - REQ-002, REQ-003, REQ-004, REQ-201, EDGE-102, NFR-101, NFR-102
- **設計文書**: [📐 architecture.md](../../design/content-edit-preview/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/content-edit-preview/dataflow.md)

## タスク概要

`EditScreen` Composable を実装する。4フィールド（タイトル・本文・タグ・フォルダ）と送信・キャンセルボタンを持つフォーム画面。`BackHandler` でバックボタンをキャンセルと同等の動作に対応する（EDGE-102）。ボタンはスクロールに関わらず常に画面下部に固定表示する（NFR-102）。

## 依存タスク

- **前提タスク**:
  - [TASK-0017: EditScreenViewModel 実装](TASK-0017.md)
  - [TASK-0018: strings.xml UI文字列リソース追加](TASK-0018.md)
- **後続タスク**: [TASK-0020: MainActivity フロー変更](TASK-0020.md)

## 完了条件

- [x] `EditScreen.kt` が `ui/` パッケージに作成されている
- [x] タイトル・本文・タグ・フォルダの4フィールドが表示される
- [x] 送信ボタンタップで `onSend` コールバックが呼ばれる
- [x] キャンセルボタンタップで `onCancel` コールバックが呼ばれる
- [x] バックボタンで `onCancel` コールバックが呼ばれる（EDGE-102）
- [x] ボタンが画面下部に固定表示される（NFR-102）
- [x] `./gradlew assembleDebug` がエラーなく通過する

---

## 実装詳細

### 1. EditScreen Composable の基本構造 🔵

**信頼性**: 🔵 *REQ-002, REQ-003, REQ-004・NFR-101, NFR-102より*

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/ui/EditScreen.kt`

```kotlin
package com.den4dr.share2Obsidian.ui

import androidx.activity.compose.BackHandler
import androidx.compose.foundation.layout.*
import androidx.compose.foundation.rememberScrollState
import androidx.compose.foundation.verticalScroll
import androidx.compose.material3.*
import androidx.compose.runtime.*
import androidx.compose.ui.Modifier
import androidx.compose.ui.res.stringResource
import androidx.compose.ui.unit.dp
import com.den4dr.share2Obsidian.R

@Composable
fun EditScreen(
    viewModel: EditScreenViewModel,
    onSend: (SendParams) -> Unit,
    onCancel: () -> Unit
) {
    val formState by viewModel.formState.collectAsState()

    // バックボタン対応（EDGE-102）
    BackHandler { onCancel() }

    Scaffold(
        bottomBar = {
            // ボタンを画面下部に固定（NFR-102）
            Row(
                modifier = Modifier
                    .fillMaxWidth()
                    .padding(16.dp),
                horizontalArrangement = Arrangement.spacedBy(8.dp)
            ) {
                OutlinedButton(
                    onClick = onCancel,
                    modifier = Modifier.weight(1f)
                ) {
                    Text(stringResource(R.string.button_cancel))
                }
                Button(
                    onClick = {
                        val config = /* NoteConfigをどこから取得するか 後述 */
                        onSend(viewModel.buildSendParams(config))
                    },
                    modifier = Modifier.weight(1f)
                ) {
                    Text(stringResource(R.string.button_send))
                }
            }
        }
    ) { paddingValues ->
        Column(
            modifier = Modifier
                .fillMaxSize()
                .padding(paddingValues)
                .padding(16.dp)
                .verticalScroll(rememberScrollState()),
            verticalArrangement = Arrangement.spacedBy(12.dp)
        ) {
            OutlinedTextField(
                value = formState.title,
                onValueChange = { viewModel.updateTitle(it) },
                label = { Text(stringResource(R.string.label_title)) },
                modifier = Modifier.fillMaxWidth(),
                singleLine = true  // タイトルは単一行（NFR-101）
            )
            OutlinedTextField(
                value = formState.body,
                onValueChange = { viewModel.updateBody(it) },
                label = { Text(stringResource(R.string.label_body)) },
                modifier = Modifier.fillMaxWidth(),
                minLines = 5  // 本文は複数行（NFR-101）
            )
            OutlinedTextField(
                value = formState.tagsText,
                onValueChange = { viewModel.updateTagsText(it) },
                label = { Text(stringResource(R.string.label_tags)) },
                modifier = Modifier.fillMaxWidth(),
                singleLine = true
            )
            OutlinedTextField(
                value = formState.folder,
                onValueChange = { viewModel.updateFolder(it) },
                label = { Text(stringResource(R.string.label_folder)) },
                modifier = Modifier.fillMaxWidth(),
                singleLine = true
            )
        }
    }
}
```

**注意**: `NoteConfig` の受け渡し方法は実装時に判断（EditScreen に渡す or ViewModel に保持する）。

### 2. BackHandler によるバックボタン対応 🔵

**信頼性**: 🔵 *EDGE-102・ユーザヒアリング（バックボタン対応）より*

`BackHandler { onCancel() }` を EditScreen 内に追加し、Android バックボタンをキャンセルと同等の動作にする。

---

## UI/UX要件

### レイアウト 🔵

**信頼性**: 🔵 *REQ-003, REQ-004・NFR-101, NFR-102より*

- タイトル: `OutlinedTextField` + `singleLine = true`
- 本文: `OutlinedTextField` + `minLines = 5`（複数行）
- タグ: `OutlinedTextField` + `singleLine = true`（カンマ区切り文字列）
- フォルダ: `OutlinedTextField` + `singleLine = true`
- キャンセル・送信ボタン: `Scaffold.bottomBar` に配置して画面下部固定

### スクロール対応 🟡

**信頼性**: 🟡 *NFR-102・モバイル UX から妥当な推測*

- フィールド部分は `verticalScroll` でスクロール可能
- ボタンは `Scaffold.bottomBar` で固定表示

---

## 単体テスト要件（Compose UI Test）

**テストファイル**: `app/src/androidTest/java/com/den4dr/share2Obsidian/ui/EditScreenTest.kt`

### テストケース1: フィールドの初期値表示 🔵

**信頼性**: 🔵 *TC-003-01, TC-003-03, TC-003-04より*

**Given**: ViewModel が初期化済み（title="テスト", body="本文", tagsText="shared", folder="70_clippings"）
**When**: EditScreen を表示
**Then**: 各フィールドに対応する初期値が表示される

### テストケース2: 送信ボタンで onSend が呼ばれる 🔵

**信頼性**: 🔵 *REQ-101・TC-101-01より*

**Given**: EditScreen が表示されている
**When**: 送信ボタンをタップ
**Then**: onSend コールバックが呼ばれる

### テストケース3: キャンセルボタンで onCancel が呼ばれる 🔵

**信頼性**: 🔵 *REQ-201・TC-201-01より*

**Given**: EditScreen が表示されている
**When**: キャンセルボタンをタップ
**Then**: onCancel コールバックが呼ばれる（startActivity は呼ばれない）

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-0019` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- Compose のインポートは `androidx.compose.material3.*` を使用（既存テーマと統一）
- `NoteConfig` の受け渡し: EditScreen の引数で渡す、または ViewModel に持たせる（実装時に判断）
- `androidTest` で Compose UI Test を実行するため、デバイス/エミュレータが必要

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 2 | 0 | 0 | 2 |
| UI/UX要件 | 1 | 1 | 0 | 2 |
| 単体テスト | 3 | 0 | 0 | 3 |

- **総項目数**: 7項目
- 🔵 **青信号**: 6項目 (86%)
- 🟡 **黄信号**: 1項目 (14%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質
