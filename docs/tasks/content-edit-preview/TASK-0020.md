# TASK-0020: MainActivity フロー変更

**タスクID**: TASK-0020
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 2 - UI・統合実装
**信頼性レベル**: 🔵 *REQ-001, REQ-101, REQ-201, REQ-301・既存実装・dataflow.mdより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/content-edit-preview/requirements.md) - REQ-001, REQ-101, REQ-102, REQ-201, REQ-301, REQ-401
- **設計文書**: [📐 architecture.md](../../design/content-edit-preview/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/content-edit-preview/dataflow.md)

## タスク概要

`MainActivity` のフローを変更する。処理完了後の即時 Obsidian 起動を撤廃し、`EditScreen` を表示するよう変更する。送信・キャンセルのコールバックを実装し、`NoteComposer` 経由で URI を構築して Obsidian を起動する。

**変更前フロー**: 処理完了 → FrontmatterBuilder → ObsidianUriBuilder → startActivity → finish()
**変更後フロー**: 処理完了 → viewModel.initialize() → setContent(EditScreen) → onSend → NoteComposer → startActivity → finish()

## 依存タスク

- **前提タスク**:
  - [TASK-0015: NoteConfig + NoteComposer 実装](TASK-0015.md)
  - [TASK-0019: EditScreen Composable 実装](TASK-0019.md)
- **後続タスク**: なし（最終タスク）

## 完了条件 ✅ **完了** (TDD開発完了 - 10テストケース全通過)

- [x] `MainActivity` が `EditScreen` を表示するよう変更されている
- [x] 送信ボタンで Obsidian URI が構築され `startActivity` が呼ばれる
- [x] キャンセルボタンで `startActivity` を呼ばずに `finish()` が呼ばれる
- [x] バックボタンで `finish()` が呼ばれる（EDGE-102）
- [x] URL処理: LoadingScreen → EditScreen の遷移が動作する
- [x] Obsidian 未インストール時にトーストが表示される
- [x] `./gradlew assembleDebug` がエラーなく通過する

---

## 実装詳細

### 1. MainActivity のフロー変更 🔵

**信頼性**: 🔵 *REQ-001, REQ-401・dataflow.md・既存実装より*

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/MainActivity.kt`

```kotlin
class MainActivity : ComponentActivity() {
    private val viewModel: EditScreenViewModel by viewModels()

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)

        val shareContent = ContentTypeDetector.detect(intent)
        if (shareContent == null) {
            finish()
            return
        }

        // URL の場合は処理中にローディング画面を表示（REQ-301、変更なし）
        if (shareContent is ShareContent.Url) {
            setContent { LoadingScreen() }
        }

        val config = NoteConfig.fromAppConfig()

        lifecycleScope.launch {
            val processed = when (shareContent) {
                is ShareContent.Text -> TextContentProcessor().process(shareContent)
                is ShareContent.Url -> UrlContentProcessor(WebViewExtractor(this@MainActivity)).process(shareContent)
                is ShareContent.Html -> HtmlContentProcessor().process(shareContent)
                is ShareContent.File -> FileContentProcessor(this@MainActivity).process(shareContent)
            }

            viewModel.initialize(processed, config)

            setContent {
                EditScreen(
                    viewModel = viewModel,
                    onSend = { sendParams ->
                        val content = NoteComposer.buildFrontmatter(
                            sendParams.title, sendParams.body, sendParams.tags
                        )
                        val uri = NoteComposer.buildUri(content, sendParams.title, sendParams.config)
                        try {
                            startActivity(Intent(Intent.ACTION_VIEW, uri))
                        } catch (e: ActivityNotFoundException) {
                            Toast.makeText(
                                this@MainActivity,
                                getString(R.string.error_obsidian_not_installed),
                                Toast.LENGTH_LONG
                            ).show()
                        }
                        finish()
                    },
                    onCancel = { finish() }
                )
            }
        }
    }
}
```

### 2. 旧コードの削除 🔵

**信頼性**: 🔵 *REQ-001・dataflow.md より*

既存の以下のコードを削除する:
- `FrontmatterBuilder.build(processed.title, processed.body)` の呼び出し
- `ObsidianUriBuilder.build(noteContent, processed.title)` の呼び出し
- coroutine 内の `startActivity` 直接呼び出し
- coroutine 内の `finish()` 直接呼び出し

---

## 単体テスト要件

**テストファイル**: `app/src/androidTest/java/com/den4dr/share2Obsidian/MainActivityEditFlowTest.kt`

### テストケース1: キャンセル後に Activity が終了する 🔵

**信頼性**: 🔵 *TC-201-01・REQ-201より*

**Given**: テキスト共有インテントで MainActivity が起動している
**When**: キャンセルボタンをタップ
**Then**: Activity が終了し、Obsidian への `startActivity` が呼ばれていない

### テストケース2: 送信後に Obsidian URI が正しく構築される 🔵

**信頼性**: 🔵 *TC-101-01・REQ-101より*

**Given**: タイトル "テスト"、本文 "本文"、タグ "shared, web"、フォルダ "70_clippings" で編集後
**When**: 送信ボタンをタップ
**Then**: `obsidian://new?content=...&title=テスト&vault=...&folder=70_clippings` の URI で startActivity が呼ばれる

### テストケース3: Obsidian 未インストール時にトーストが表示される 🔵

**信頼性**: 🔵 *TC-101-E01・既存 REQ-401 実装より*

**Given**: Obsidian がインストールされていない環境
**When**: 送信ボタンをタップ
**Then**: `ActivityNotFoundException` がキャッチされ、トーストが表示され、Activity が終了する

---

## 統合テスト要件

### URL フロー: LoadingScreen → EditScreen 遷移 🔵

**信頼性**: 🔵 *REQ-301・TC-301-01より*

**テスト内容**: URL 共有インテントで起動した場合、LoadingScreen が表示された後 EditScreen に切り替わること
**期待結果**: ローディング表示 → 編集画面表示（本文フィールドに抽出テキストが入力済み）

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-0020` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- `FrontmatterBuilder` / `ObsidianUriBuilder` の直接呼び出しは削除するが、ファイル自体は残す（REQ-402）
- `viewModels()` デリゲートを使うため `implementation(libs.androidx.activity.compose)` が build.gradle.kts に含まれていることを確認
- `lifecycleScope.launch` は `Dispatchers.Main` で動作するため、setContent() はそのまま呼び出せる
- URL 処理: LoadingScreen は setContent() で最初に表示し、処理完了後に setContent() で EditScreen に置き換える

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 2 | 0 | 0 | 2 |
| 単体テスト | 3 | 0 | 0 | 3 |
| 統合テスト | 1 | 0 | 0 | 1 |

- **総項目数**: 6項目
- 🔵 **青信号**: 6項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質
