# TASK-0016: EditFormState + parseTagsText + SendParams 実装 ✅ **完了** (TDD開発完了 - 19テストケース全通過)

**タスクID**: TASK-0016
**タスクタイプ**: TDD
**推定工数**: 3時間
**フェーズ**: Phase 1 - 基盤実装
**信頼性レベル**: 🔵 *要件定義書REQ-003, REQ-103・設計文書interfaces.ktより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/content-edit-preview/requirements.md) - REQ-003, REQ-101, REQ-103
- **設計文書**: [📐 architecture.md](../../design/content-edit-preview/architecture.md)
- **インターフェース定義**: [📝 interfaces.kt](../../design/content-edit-preview/interfaces.kt)

## タスク概要

フォーム状態管理と送信パラメータのデータクラス、タグパース関数を実装する。
- `EditFormState`: 編集フォームの状態（title, body, tagsText, folder）
- `parseTagsText()`: カンマ区切りタグ文字列 → `List<String>` 変換（REQ-103）
- `SendParams`: 送信ボタンタップ時の送信パラメータ（NoteConfig込み）

## 依存タスク

- **前提タスク**: なし（TASK-0015 と並列実行可能）
- **後続タスク**: [TASK-0017: EditScreenViewModel 実装](TASK-0017.md)

## 完了条件

- [x] `EditFormState.kt` が作成されている
- [x] `parseTagsText()` 関数が実装されている
- [x] `SendParams.kt` が作成されている
- [x] `ParseTagsTextTest.kt` のすべてのテストがパスする
- [x] `./gradlew test` がエラーなく通過する

---

## 実装詳細

### 1. EditFormState データクラス 🔵

**信頼性**: 🔵 *REQ-003（4フィールド）・ユーザヒアリングより*

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/ui/EditFormState.kt`

```kotlin
package com.den4dr.share2Obsidian.ui

data class EditFormState(
    val title: String,
    val body: String,
    val tagsText: String,  // カンマ区切り文字列 例: "shared, web"
    val folder: String
)
```

### 2. parseTagsText 関数 🔵

**信頼性**: 🔵 *REQ-103・ユーザヒアリング（カンマ区切り、スペーストリム）より*

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/ui/EditFormState.kt` (同ファイルに追加)

```kotlin
fun parseTagsText(tagsText: String): List<String> {
    return tagsText
        .split(",")
        .map { it.trim() }
        .filter { it.isNotEmpty() }
}
```

### 3. SendParams データクラス 🔵

**信頼性**: 🔵 *REQ-101・dataflow.md の送信フローより*

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/ui/SendParams.kt`

```kotlin
package com.den4dr.share2Obsidian.ui

import com.den4dr.share2Obsidian.format.NoteConfig

data class SendParams(
    val title: String?,          // 空文字の場合は null（EDGE-001）
    val body: String,
    val tags: List<String>,      // parseTagsText() 適用済み
    val config: NoteConfig
)
```

---

## 単体テスト要件

**テストファイル**: `app/src/test/java/com/den4dr/share2Obsidian/ui/ParseTagsTextTest.kt`

### テストケース1: 通常のカンマ区切り 🔵

**信頼性**: 🔵 *REQ-103・acceptance-criteria.md TC-101-02より*

**Given**: "shared, web, clipping"
**When**: parseTagsText(input)
**Then**: ["shared", "web", "clipping"]

### テストケース2: スペースのトリム 🔵

**信頼性**: 🔵 *REQ-103・acceptance-criteria.md TC-101-03より*

**Given**: "shared ,  web , clipping "
**When**: parseTagsText(input)
**Then**: ["shared", "web", "clipping"]

### テストケース3: 空文字 🟡

**信頼性**: 🟡 *EDGE-003・acceptance-criteria.md TC-103-01から妥当な推測*

**Given**: ""
**When**: parseTagsText(input)
**Then**: []

### テストケース4: カンマのみ 🟡

**信頼性**: 🟡 *acceptance-criteria.md TC-103-02から妥当な推測*

**Given**: ","
**When**: parseTagsText(input)
**Then**: []

### テストケース5: スペースのみのカンマ区切り 🟡

**信頼性**: 🟡 *EDGE-003から妥当な推測*

**Given**: "  ,  ,  "
**When**: parseTagsText(input)
**Then**: []

### テストケース6: 単一タグ 🔵

**信頼性**: 🔵 *REQ-103・通常ケースとして確実*

**Given**: "shared"
**When**: parseTagsText(input)
**Then**: ["shared"]

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-0016` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- `EditFormState` は `ui` パッケージに配置する
- `parseTagsText()` は `EditFormState.kt` と同ファイルのトップレベル関数として実装する
- `SendParams` の `title` は空文字 `""` の場合 `null` に変換するロジックは `EditScreenViewModel.buildSendParams()` 側で行う（TASK-0017の責務）

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 3 | 0 | 0 | 3 |
| 単体テスト | 3 | 3 | 0 | 6 |

- **総項目数**: 9項目
- 🔵 **青信号**: 6項目 (67%)
- 🟡 **黄信号**: 3項目 (33%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質
