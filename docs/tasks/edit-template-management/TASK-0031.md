# TASK-0031: TemplateEditViewModel 実装

**タスクID**: TASK-0031
**タスクタイプ**: TDD
**推定工数**: 8時間
**フェーズ**: Phase 2 - テンプレート管理UI
**信頼性レベル**: 🔵 *interfaces.kt TemplateEditViewModel シグネチャ・REQ-012〜014, REQ-021〜022より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/edit-template-management/requirements.md)
- **設計文書**: [📐 architecture.md](../../design/edit-template-management/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/edit-template-management/dataflow.md)
- **インターフェース定義**: [📝 interfaces.kt](../../design/edit-template-management/interfaces.kt)

## タスク概要

`TemplateEditViewModel` を実装する。SavedStateHandle によるテンプレートID管理、新規/編集モード判定、テンプレート情報の各更新メソッド、`save()` の Dispatchers.IO 実行、および `isSaved=true` への状態遷移を含む。MockK を使った単体テストも作成する。

## 依存タスク

- **前提タスク**: TASK-0028
- **後続タスク**: TASK-0032

## 完了条件

- [ ] `TemplateEditViewModel` が @HiltViewModel + SavedStateHandle で定義
- [ ] 新規/編集モード判定（templateId=null で新規）
- [ ] テンプレート情報の各更新メソッドが uiState を正しく更新
- [ ] `save()` が Dispatchers.IO で saveTemplate を呼ぶ
- [ ] `isSaved=true` になったら onNavigateBack を呼ぶ仕組み
- [ ] MockK を使った ViewModel の単体テストが通る

---

## 実装詳細

### 1. TemplateEditUiState

**信頼性**: 🔵 *interfaces.kt より*

```kotlin
data class TemplateEditUiState(
    val templateId: Long? = null,
    val name: String = "",
    val vault: String = "",
    val folder: String = "",
    val isDefault: Boolean = false,
    val fields: List<TemplateFieldEditState> = emptyList(),
    val isSaving: Boolean = false,
    val errorMessage: String? = null,
    val isSaved: Boolean = false,
)
```

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/ui/template/TemplateEditViewModel.kt`

---

### 2. TemplateFieldEditState

**信頼性**: 🔵 *interfaces.kt より*

```kotlin
data class TemplateFieldEditState(
    val id: Long = 0,
    val key: String = "",
    val valueSource: FieldValueSource = FieldValueSource.EMPTY,
    val valueType: FieldValueType = FieldValueType.STRING,
    val defaultValue: String = "",
    val metaKey: HtmlMetaKey? = null,
    val sortOrder: Int = 0,
)
```

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/ui/template/TemplateEditViewModel.kt`

---

### 3. TemplateEditViewModel

**信頼性**: 🔵 *interfaces.kt TemplateEditViewModel シグネチャより*

```kotlin
@HiltViewModel
class TemplateEditViewModel @Inject constructor(
    private val templateRepository: TemplateRepository,
    savedStateHandle: SavedStateHandle
) : ViewModel() {
    // templateId: SavedStateHandle から取得、-1L の場合は新規（null として扱う）
    // _uiState / uiState
    // init: templateId != null の場合 loadTemplate(templateId)

    fun updateName(name: String)
    fun updateVault(vault: String)
    fun updateFolder(folder: String)
    fun updateIsDefault(isDefault: Boolean)
    fun addField(field: TemplateFieldEditState)
    fun removeField(index: Int)
    fun updateField(index: Int, field: TemplateFieldEditState)
    fun save()  // Dispatchers.IO で saveTemplate → isSaved = true
}
```

SavedStateHandle のキー: `"templateId"`（Long型）。`-1L` は新規テンプレート作成を意味する定数として定義する。

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/ui/template/TemplateEditViewModel.kt`

---

## 単体テスト要件

**テストファイル**: `app/src/test/java/com/den4dr/share2Obsidian/ui/template/TemplateEditViewModelTest.kt`

### テストケース1: updateName が uiState.name を更新

**信頼性**: 🔵 *interfaces.kt TemplateEditViewModel シグネチャより*

**Given**: TemplateEditViewModel（新規モード）
**When**: updateName("Web記事")
**Then**: uiState.name == "Web記事"

---

### テストケース2: addField がフィールドをリストに追加

**信頼性**: 🔵 *interfaces.kt TemplateEditViewModel シグネチャより*

**Given**: TemplateEditViewModel（新規モード、fields=empty）
**When**: addField(TemplateFieldEditState(key="source", valueSource=URL))
**Then**: uiState.fields.size == 1

---

### テストケース3: save() で repository.saveTemplate が呼ばれ isSaved=true になる

**信頼性**: 🔵 *interfaces.kt TemplateEditViewModel シグネチャより*

**Given**: 入力済みの TemplateEditViewModel
**When**: save()
**Then**: repository.saveTemplate が呼ばれ、uiState.isSaved == true

---

### テストケース4: 既存テンプレート編集時に loadTemplate でフィールドが設定される

**信頼性**: 🔵 *interfaces.kt TemplateEditViewModel シグネチャより*

**Given**: repository.getTemplateById が Template を返す
**When**: TemplateEditViewModel(templateId=1)
**Then**: uiState にテンプレート情報が反映

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-0031` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- SavedStateHandle のキー: "templateId"（Long型）
- -1L は新規テンプレート作成を意味する定数として定義
- isSaved が true になったタイミングで Screen 側が onNavigateBack() を呼ぶ
- Gradleコマンドは必ず `mise exec -- ./gradlew ...` で実行すること

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 3 | 0 | 0 | 3 |
| 単体テスト | 4 | 0 | 0 | 4 |
| UI/UX要件 | 0 | 0 | 0 | 0 |

- **総項目数**: 7項目
- 🔵 **青信号**: 7項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質
