# TASK-0036: WebViewExtractor JS 拡張 + UrlContentProcessor メタデータ設定

**タスクID**: TASK-0036
**タスクタイプ**: TDD
**推定工数**: 8時間
**フェーズ**: Phase 3 - HTMLメタデータ抽出
**信頼性レベル**: 🔵 *architecture.md WebView メタデータ抽出設計・REQ-071より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/edit-template-management/requirements.md)
- **設計文書**: [📐 architecture.md](../../design/edit-template-management/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/edit-template-management/dataflow.md)
- **インターフェース定義**: [📝 interfaces.kt](../../design/edit-template-management/interfaces.kt)

## タスク概要

`WebViewExtractor` の JavaScript を拡張して JSON 形式でメタデータを一括取得できるようにし、`AndroidBridge.onExtracted(json)` 経由で `WebViewExtractionResult` に og:title, og:description 等を設定する。また、`UrlContentProcessor` が `ProcessedContent.metadata` に `HtmlMetaKey` でマッピングする処理を実装する。

## 依存タスク

- **前提タスク**: TASK-0035
- **後続タスク**: TASK-0041

## 完了条件

- [ ] `WebViewExtractor` の JavaScript が JSON 形式でメタデータを取得する
- [ ] `AndroidBridge.onExtracted(json)` でコールバックを受け取る
- [ ] `WebViewExtractionResult` に ogTitle, ogDescription 等が設定される
- [ ] `UrlContentProcessor` が ProcessedContent.metadata に HtmlMetaKey でマッピングする
- [ ] 単体テストが通る

---

## 実装詳細

### 1. WebViewExtractor.kt の JavaScript 拡張

**信頼性**: 🔵 *architecture.md WebView設計より*

現在 `document.body.innerText` のみ取得している箇所を、JSON 形式でメタデータを一括取得する実装に変更する。

```javascript
(function() {
    function getMeta(selectors) {
        for (var s of selectors) {
            var el = document.querySelector(s);
            if (el) {
                var v = el.getAttribute('content') || el.getAttribute('value') || el.innerText;
                if (v && v.trim()) return v.trim();
            }
        }
        return '';
    }
    var result = {
        body:          document.body ? document.body.innerText : '',
        ogTitle:       getMeta(["meta[property='og:title']", "meta[name='twitter:title']", "title"]),
        ogDescription: getMeta(["meta[property='og:description']", "meta[name='description']"]),
        publishedTime: getMeta(["meta[property='article:published_time']", "meta[itemprop='datePublished']"]),
        modifiedTime:  getMeta(["meta[property='article:modified_time']", "meta[itemprop='dateModified']"]),
        author:        getMeta(["meta[name='author']", "meta[property='article:author']", "meta[property='og:site_name']"])
    };
    AndroidBridge.onExtracted(JSON.stringify(result));
})();
```

`onExtracted(json)` で JSON をパースして WebViewExtractionResult に変換する。

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/util/WebViewExtractor.kt`

---

### 2. UrlContentProcessor.kt のメタデータ設定

**信頼性**: 🔵 *dataflow.md フロー1より*

WebViewExtractionResult から ProcessedContent.metadata に変換する:

```kotlin
metadata = mapOf(
    HtmlMetaKey.OG_TITLE to result.ogTitle,
    HtmlMetaKey.OG_DESCRIPTION to result.ogDescription,
    HtmlMetaKey.URL to url,
    HtmlMetaKey.PUBLISHED_DATE to result.publishedTime,
    HtmlMetaKey.MODIFIED_DATE to result.modifiedTime,
    HtmlMetaKey.AUTHOR to result.author,
)
sourceUrl = url
```

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/content/UrlContentProcessor.kt`

---

## 単体テスト要件

テストファイル: `app/src/test/java/com/den4dr/share2Obsidian/content/UrlContentProcessorTest.kt`

### テストケース1: WebViewExtractionResult から ProcessedContent.metadata が正しくマッピングされる

**信頼性**: 🔵 *dataflow.md フロー1より*

**Given**: ogTitle="テスト記事", author="著者名" を持つ WebViewExtractionResult
**When**: UrlContentProcessor でメタデータを設定
**Then**: `ProcessedContent.metadata[HtmlMetaKey.OG_TITLE] == "テスト記事"`

---

### テストケース2: sourceUrl が ProcessedContent.sourceUrl に設定される

**信頼性**: 🔵 *dataflow.md フロー1より*

**Given**: URL = "https://example.com"
**When**: UrlContentProcessor で処理
**Then**: `ProcessedContent.sourceUrl == "https://example.com"`

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-0036` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- WebView のテストは androidTest（実機/エミュレータ必要）だが、UrlContentProcessor の単体テストは JUnit で実施すること
- JSON パースエラー時は空のメタデータ (`emptyMap()`) として扱うこと（dataflow.md エラーハンドリングより）

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 2 | 0 | 0 | 2 |
| 単体テスト | 2 | 0 | 0 | 2 |

- **総項目数**: 4項目
- 🔵 **青信号**: 4項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)

**品質評価**: 高品質
