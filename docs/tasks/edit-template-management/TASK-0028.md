# TASK-0028: TemplateRepository・Hilt DI・Application クラス実装

**タスクID**: TASK-0028
**タスクタイプ**: TDD
**推定工数**: 8時間
**フェーズ**: Phase 1 - 基盤構築
**信頼性レベル**: 🔵 *interfaces.kt・architecture.md REQ-403, NFR-201より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/edit-template-management/requirements.md)
- **設計文書**: [📐 architecture.md](../../design/edit-template-management/architecture.md)
- **DB スキーマ**: [🗄️ database-schema.kt](../../design/edit-template-management/database-schema.kt)
- **インターフェース定義**: [📝 interfaces.kt](../../design/edit-template-management/interfaces.kt)

## タスク概要

`TemplateRepository` インターフェースと `TemplateRepositoryImpl` を実装し、Hilt DI モジュール（`DatabaseModule`）、Application クラス（`Share2ObsidianApp`）を追加する。既存の `MainActivity.kt` に `@AndroidEntryPoint` を追加することで Hilt の DI が有効になる。Repository 実装にはエンティティ ↔ ドメインモデルのマッピング拡張関数を含む。

## 依存タスク

- **前提タスク**: TASK-0026, TASK-0027
- **後続タスク**: TASK-0029, TASK-0031, TASK-0041

## 完了条件

- [ ] `Share2ObsidianApp.kt` が @HiltAndroidApp で定義
- [ ] `di/DatabaseModule.kt` が @Module @InstallIn(SingletonComponent::class) で定義
- [ ] `TemplateRepository` インターフェースが5メソッドを持つ
- [ ] `TemplateRepositoryImpl` がマッピング拡張関数を含む完全な実装
- [ ] `MainActivity.kt` に @AndroidEntryPoint が追加
- [ ] MockK を使った Repository の単体テストが通る

---

## 実装詳細

### 1. Share2ObsidianApp.kt

**信頼性**: 🔵 *architecture.md REQ-403より*

Hilt の Application クラス。`AndroidManifest.xml` の `android:name=".Share2ObsidianApp"` と対応する（TASK-0025 で設定済み）。

```kotlin
package com.den4dr.share2Obsidian

import android.app.Application
import dagger.hilt.android.HiltAndroidApp

@HiltAndroidApp
class Share2ObsidianApp : Application()
```

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/Share2ObsidianApp.kt`

---

### 2. di/DatabaseModule.kt

**信頼性**: 🔵 *interfaces.kt Hilt Module シグネチャより*

Room DB・DAO・Repository を Singleton スコープで提供する Hilt モジュール。

```kotlin
package com.den4dr.share2Obsidian.di

import android.content.Context
import androidx.room.Room
import com.den4dr.share2Obsidian.data.db.AppDatabase
import com.den4dr.share2Obsidian.data.db.TemplateDao
import com.den4dr.share2Obsidian.data.repository.TemplateRepository
import com.den4dr.share2Obsidian.data.repository.TemplateRepositoryImpl
import dagger.Module
import dagger.Provides
import dagger.hilt.InstallIn
import dagger.hilt.android.qualifiers.ApplicationContext
import dagger.hilt.components.SingletonComponent
import javax.inject.Singleton

@Module
@InstallIn(SingletonComponent::class)
object DatabaseModule {
    @Provides
    @Singleton
    fun provideAppDatabase(@ApplicationContext context: Context): AppDatabase =
        Room.databaseBuilder(context, AppDatabase::class.java, "share2obsidian.db").build()

    @Provides
    @Singleton
    fun provideTemplateDao(db: AppDatabase): TemplateDao = db.templateDao()

    @Provides
    @Singleton
    fun provideTemplateRepository(dao: TemplateDao): TemplateRepository =
        TemplateRepositoryImpl(dao)
}
```

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/di/DatabaseModule.kt`

---

### 3. TemplateRepository.kt

**信頼性**: 🔵 *interfaces.kt NFR-201より*

Repository のインターフェース定義。5つのメソッドを持つ。

```kotlin
package com.den4dr.share2Obsidian.data.repository

import com.den4dr.share2Obsidian.domain.model.Template
import kotlinx.coroutines.flow.Flow

interface TemplateRepository {
    fun getAllTemplates(): Flow<List<Template>>
    suspend fun getDefaultTemplate(): Template?
    suspend fun getTemplateById(id: Long): Template?
    suspend fun saveTemplate(template: Template): Long
    suspend fun deleteTemplate(template: Template)
}
```

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/data/repository/TemplateRepository.kt`

---

### 4. TemplateRepositoryImpl.kt

**信頼性**: 🔵 *database-schema.kt Repository実装シグネチャより*

- `@Inject constructor(private val dao: TemplateDao)` を使用
- エンティティ ↔ ドメインモデルのマッピング拡張関数（`private`）を内部に定義
- `saveTemplate`: isDefault=true の場合 `clearDefaultExcept`、`insertTemplate`、`deleteFieldsByTemplateId`、`insertFields` の順に実行
- `getAllTemplates`: `dao.getAllTemplatesWithFields().map { list -> list.map { it.toDomain() } }`

マッピング拡張関数の概要:

```kotlin
private fun TemplateWithFields.toDomain(): Template = Template(
    id = template.id,
    name = template.name,
    vault = template.vault,
    folder = template.folder,
    isDefault = template.isDefault,
    fields = fields.map { it.toDomain() },
)

private fun TemplateFieldEntity.toDomain(): TemplateField = TemplateField(
    id = id,
    templateId = templateId,
    key = key,
    valueSource = FieldValueSource.valueOf(valueSource),
    valueType = FieldValueType.valueOf(valueType),
    defaultValue = defaultValue,
    metaKey = metaKey?.let { HtmlMetaKey.valueOf(it) },
    sortOrder = sortOrder,
)

private fun Template.toEntity(): TemplateEntity = TemplateEntity(
    id = id,
    name = name,
    vault = vault,
    folder = folder,
    isDefault = isDefault,
)

private fun TemplateField.toEntity(templateId: Long): TemplateFieldEntity = TemplateFieldEntity(
    id = id,
    templateId = templateId,
    key = key,
    valueSource = valueSource.name,
    valueType = valueType.name,
    defaultValue = defaultValue,
    metaKey = metaKey?.name,
    sortOrder = sortOrder,
)
```

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/data/repository/TemplateRepositoryImpl.kt`

---

### 5. MainActivity.kt への @AndroidEntryPoint 追加

**信頼性**: 🔵 *architecture.md Hilt セットアップより*

既存の `MainActivity` クラス宣言に `@AndroidEntryPoint` アノテーションを追加する。

```kotlin
@AndroidEntryPoint
class MainActivity : ComponentActivity() {
    // 既存の実装は変更しない
}
```

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/MainActivity.kt`（既存ファイルを変更）

---

## 単体テスト要件

**テストファイル**: `app/src/test/java/com/den4dr/share2Obsidian/data/repository/TemplateRepositoryImplTest.kt`

### テストケース1: getAllTemplates が Flow を正しく変換

**信頼性**: 🔵 *interfaces.kt NFR-201より*

**Given**: MockK で `dao.getAllTemplatesWithFields()` が `TemplateWithFields` のリストを返す Flow を返す
**When**: `getAllTemplates()` を collect する
**Then**: `Template` ドメインモデルのリストに変換されている（name, vault, folder が一致）

**テストファイル**: `app/src/test/java/com/den4dr/share2Obsidian/data/repository/TemplateRepositoryImplTest.kt`

---

### テストケース2: saveTemplate で isDefault=true の場合 clearDefaultExcept が呼ばれる

**信頼性**: 🔵 *database-schema.kt REQ-022より*

**Given**: isDefault=true の Template（id=1）
**When**: `saveTemplate(template)` を呼び出す
**Then**: `dao.clearDefaultExcept(1)` が呼ばれる

**テストファイル**: `app/src/test/java/com/den4dr/share2Obsidian/data/repository/TemplateRepositoryImplTest.kt`

---

### テストケース3: deleteTemplate で dao.deleteTemplate が呼ばれる

**信頼性**: 🔵 *interfaces.kt NFR-201より*

**Given**: Template（id=5）
**When**: `deleteTemplate(template)` を呼び出す
**Then**: `dao.deleteTemplate(TemplateEntity(id=5, ...))` が呼ばれる

**テストファイル**: `app/src/test/java/com/den4dr/share2Obsidian/data/repository/TemplateRepositoryImplTest.kt`

---

### テストケース4: TemplateWithFields.toDomain() マッピング確認

**信頼性**: 🔵 *database-schema.kt Repository実装シグネチャより*

**Given**: `TemplateEntity(id=1, name="テスト", vault="v", folder="f", isDefault=true)` + `TemplateFieldEntity` リスト
**When**: `toDomain()` でドメインモデルに変換
**Then**: `Template` と `TemplateField` のすべてのフィールドが正しくマッピングされている

**テストファイル**: `app/src/test/java/com/den4dr/share2Obsidian/data/repository/TemplateRepositoryImplTest.kt`

---

## 実装手順

### TDDタスクの場合

1. `/tsumiki:tdd-requirements TASK-0028` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- `@Inject` アノテーション付きコンストラクタを使用（`TemplateRepositoryImpl` は `@Inject constructor(private val dao: TemplateDao)` と定義）
- マッピング拡張関数は `TemplateRepositoryImpl.kt` 内に `private` 関数として定義
- `saveTemplate` での isDefault=false の場合は `clearDefaultExcept` を呼ばない（`clearAllDefaults` も不要）
- `MainActivity.kt` への `@AndroidEntryPoint` 追加は最小変更（既存ロジックに手を加えない）

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 5 | 0 | 0 | 5 |
| 単体テスト | 4 | 0 | 0 | 4 |

- **総項目数**: 9項目
- 🔵 **青信号**: 9項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質
