# TASK-0029: TemplateListViewModel 実装

**タスクID**: TASK-0029
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 2 - テンプレート管理UI
**信頼性レベル**: 🔵 *interfaces.kt TemplateListViewModel シグネチャ・REQ-011より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/edit-template-management/requirements.md)
- **設計文書**: [📐 architecture.md](../../design/edit-template-management/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/edit-template-management/dataflow.md)
- **インターフェース定義**: [📝 interfaces.kt](../../design/edit-template-management/interfaces.kt)

## タスク概要

`TemplateListViewModel` を実装する。Hilt DI による `TemplateRepository` の注入、`getAllTemplates()` を Flow で購読する `uiState: StateFlow<TemplateListUiState>` の提供、および `deleteTemplate()` メソッドの実装を行う。MockK を使った単体テストも作成する。

## 依存タスク

- **前提タスク**: TASK-0028
- **後続タスク**: TASK-0030

## 完了条件

- [ ] `TemplateListViewModel` が @HiltViewModel で定義
- [ ] `uiState: StateFlow<TemplateListUiState>` が getAllTemplates() を Flow で購読
- [ ] `deleteTemplate(template: Template)` が viewModelScope + Dispatchers.IO で実行
- [ ] MockK を使った ViewModel の単体テストが通る

---

## 実装詳細

### 1. TemplateListUiState

**信頼性**: 🔵 *interfaces.kt より*

```kotlin
data class TemplateListUiState(
    val templates: List<Template> = emptyList(),
    val isLoading: Boolean = false,
    val errorMessage: String? = null,
)
```

TemplateListViewModel.kt 内に定義するか、別ファイルで定義する。

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/ui/template/TemplateListViewModel.kt`

---

### 2. TemplateListViewModel

**信頼性**: 🔵 *interfaces.kt TemplateListViewModel シグネチャより*

```kotlin
@HiltViewModel
class TemplateListViewModel @Inject constructor(
    private val templateRepository: TemplateRepository
) : ViewModel() {
    val uiState: StateFlow<TemplateListUiState> = templateRepository
        .getAllTemplates()
        .map { templates -> TemplateListUiState(templates = templates) }
        .stateIn(viewModelScope, SharingStarted.WhileSubscribed(5000), TemplateListUiState())

    fun deleteTemplate(template: Template) {
        viewModelScope.launch(Dispatchers.IO) {
            templateRepository.deleteTemplate(template)
        }
    }
}
```

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/ui/template/TemplateListViewModel.kt`

---

## 単体テスト要件

**テストファイル**: `app/src/test/java/com/den4dr/share2Obsidian/ui/template/TemplateListViewModelTest.kt`

### テストケース1: uiState がテンプレートリストを反映

**信頼性**: 🔵 *interfaces.kt TemplateListViewModel シグネチャより*

**Given**: TemplateRepository.getAllTemplates() が Template リストを返す
**When**: uiState.value を取得
**Then**: templates が設定されている

---

### テストケース2: deleteTemplate が repository.deleteTemplate を呼ぶ

**信頼性**: 🔵 *interfaces.kt TemplateListViewModel シグネチャより*

**Given**: MockK の TemplateRepository
**When**: deleteTemplate(template)
**Then**: repository.deleteTemplate(template) が呼ばれる

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-0029` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- SharingStarted.WhileSubscribed(5000) は Android ViewModel のベストプラクティス
- Gradleコマンドは必ず `mise exec -- ./gradlew ...` で実行すること

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 2 | 0 | 0 | 2 |
| 単体テスト | 2 | 0 | 0 | 2 |
| UI/UX要件 | 0 | 0 | 0 | 0 |

- **総項目数**: 4項目
- 🔵 **青信号**: 4項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質
