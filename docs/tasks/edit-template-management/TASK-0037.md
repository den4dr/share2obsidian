# TASK-0037: HtmlContentProcessor Jsoup メタデータ抽出実装

**タスクID**: TASK-0037
**タスクタイプ**: TDD
**推定工数**: 6時間
**フェーズ**: Phase 3 - HTMLメタデータ抽出
**信頼性レベル**: 🔵 *architecture.md Jsoup メタデータ抽出設計・REQ-071より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/edit-template-management/requirements.md)
- **設計文書**: [📐 architecture.md](../../design/edit-template-management/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/edit-template-management/dataflow.md)
- **インターフェース定義**: [📝 interfaces.kt](../../design/edit-template-management/interfaces.kt)

## タスク概要

`HtmlContentProcessor` に `extractMetadata(doc, sourceUrl)` メソッドを追加し、Jsoup を使って og:title, og:description, URL, published_time, modified_time, author を抽出する。既存の `process()` メソッドを変更して metadata と sourceUrl を `ProcessedContent` に設定する。

## 依存タスク

- **前提タスク**: TASK-0035
- **後続タスク**: TASK-0041

## 完了条件

- [ ] `HtmlContentProcessor` に `extractMetadata(doc, sourceUrl)` メソッドが追加
- [ ] og:title, og:description, URL, published_time, modified_time, author が抽出される
- [ ] `ProcessedContent` の metadata と sourceUrl が設定される
- [ ] 単体テストが通る

---

## 実装詳細

### 1. HtmlContentProcessor.kt の extractMetadata 追加

**信頼性**: 🔵 *architecture.md Jsoup メタデータ抽出設計より*

```kotlin
private fun extractMetadata(doc: org.jsoup.nodes.Document, sourceUrl: String?): Map<HtmlMetaKey, String> {
    fun meta(vararg selectors: String): String {
        for (sel in selectors) {
            val v = doc.select(sel).attr("content").trim()
            if (v.isNotEmpty()) return v
        }
        return ""
    }
    return mapOf(
        HtmlMetaKey.OG_TITLE to meta("meta[property=og:title]").ifEmpty { doc.title() },
        HtmlMetaKey.OG_DESCRIPTION to meta("meta[property=og:description]", "meta[name=description]"),
        HtmlMetaKey.URL to (sourceUrl ?: ""),
        HtmlMetaKey.PUBLISHED_DATE to meta("meta[property=article:published_time]", "meta[itemprop=datePublished]"),
        HtmlMetaKey.MODIFIED_DATE to meta("meta[property=article:modified_time]", "meta[itemprop=dateModified]"),
        HtmlMetaKey.AUTHOR to meta("meta[name=author]", "meta[property=article:author]", "meta[property=og:site_name]")
    )
}
```

既存の `process()` メソッドを変更して metadata と sourceUrl を ProcessedContent に設定する。

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/content/HtmlContentProcessor.kt`

---

## 単体テスト要件

テストファイル: `app/src/test/java/com/den4dr/share2Obsidian/content/HtmlContentProcessorTest.kt`

### テストケース1: og:title タグからメタデータが抽出される

**信頼性**: 🔵 *architecture.md Jsoup メタデータ抽出設計より*

**Given**: `<meta property="og:title" content="テスト記事">` を含む HTML
**When**: `process()`
**Then**: `metadata[OG_TITLE] == "テスト記事"`

---

### テストケース2: og:title がない場合は `<title>` タグを使用

**信頼性**: 🔵 *architecture.md Jsoup メタデータ抽出設計より*

**Given**: og:title なし、`<title>ページタイトル</title>` を含む HTML
**When**: `process()`
**Then**: `metadata[OG_TITLE] == "ページタイトル"`

---

### テストケース3: メタデータが一切ない場合は空文字列

**信頼性**: 🔵 *architecture.md Jsoup メタデータ抽出設計より*

**Given**: メタデータタグのない HTML
**When**: `process()`
**Then**: `metadata[OG_DESCRIPTION] == ""`

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-0037` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- Jsoup 1.18.3 は既に `libs.versions.toml` に導入済みのため追加依存不要
- HTML 共有時は sourceUrl = null（EDGE-003: metadata[URL] == ""）

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 1 | 0 | 0 | 1 |
| 単体テスト | 3 | 0 | 0 | 3 |

- **総項目数**: 4項目
- 🔵 **青信号**: 4項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)

**品質評価**: 高品質
