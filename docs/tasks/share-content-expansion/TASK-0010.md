# TASK-0010: WebViewExtractor実装とテスト

**タスクID**: TASK-0010
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 5 - URL処理・UI実装
**信頼性レベル**: 🔵 *dataflow.md フロー2・REQ-101/102/103・ユーザヒアリング（WebView）より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/share-content-expansion/requirements.md) - REQ-101, REQ-102, REQ-103, NFR-001
- **設計文書**: [📐 architecture.md](../../design/share-content-expansion/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/share-content-expansion/dataflow.md)

## タスク概要

非表示 WebView と JavascriptInterface を使って URL 先の本文テキスト（`document.body.innerText`）を抽出する `WebViewExtractor` を実装する。10秒タイムアウト内に取得できない場合は null を返す。`suspendCancellableCoroutine` を使用する。

## 依存タスク

- **前提タスク**: [TASK-0001: 依存パッケージ追加](TASK-0001.md), [TASK-0002: データクラス定義](TASK-0002.md)
- **後続タスク**: [TASK-0011: UrlContentProcessor実装](TASK-0011.md)

## 完了条件

- [ ] `util/WebViewExtractor.kt` が実装されている
- [ ] `WebView` を非表示で生成・使用している（`WRAP_CONTENT` + `visibility=GONE` or in-memory）
- [ ] `onPageFinished` で JS `document.body.innerText` を実行している
- [ ] `JavascriptInterface` で結果を受け取り `suspendCancellableCoroutine` を resume している
- [ ] `AppConfig.WEBVIEW_TIMEOUT_MS`（10000ms）でタイムアウト → null を返す
- [ ] `javaScriptEnabled = true` が設定されている
- [ ] メインスレッドで WebView を操作している（Android 制約）

---

## 実装詳細

### 1. WebViewExtractor クラス 🔵

**信頼性**: 🔵 *dataflow.md フロー2・REQ-101/102/103・ユーザヒアリングより*

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/util/WebViewExtractor.kt`

```kotlin
package com.den4dr.share2Obsidian.util

import android.annotation.SuppressLint
import android.content.Context
import android.webkit.JavascriptInterface
import android.webkit.WebView
import android.webkit.WebViewClient
import com.den4dr.share2Obsidian.AppConfig
import kotlinx.coroutines.suspendCancellableCoroutine
import kotlinx.coroutines.withTimeoutOrNull
import kotlin.coroutines.resume

class WebViewExtractor(private val context: Context) {

    @SuppressLint("SetJavaScriptEnabled")
    suspend fun extract(url: String): WebViewExtractionResult {
        return withTimeoutOrNull(AppConfig.WEBVIEW_TIMEOUT_MS) {
            suspendCancellableCoroutine { continuation ->
                val webView = WebView(context)
                webView.settings.javaScriptEnabled = true

                val jsInterface = object {
                    @JavascriptInterface
                    fun onTextExtracted(text: String) {
                        if (continuation.isActive) {
                            continuation.resume(WebViewExtractionResult(bodyText = text))
                        }
                    }
                }
                webView.addJavascriptInterface(jsInterface, "AndroidBridge")

                webView.webViewClient = object : WebViewClient() {
                    override fun onPageFinished(view: WebView?, url: String?) {
                        view?.evaluateJavascript(
                            "AndroidBridge.onTextExtracted(document.body.innerText)"
                        ) { }
                    }
                }
                webView.loadUrl(url)

                continuation.invokeOnCancellation {
                    webView.destroy()
                }
            }
        } ?: WebViewExtractionResult(bodyText = null)
    }
}
```

### 2. テスト上の注意 🟡

**信頼性**: 🟡 *Android WebView テストの制約から妥当な推測*

WebView は実機/エミュレータが必要なため、単体テストでは WebViewExtractor をモック化する。統合テストはインストルメンテーションテストとして実施する。

---

## 単体テスト要件

### テストケース1: タイムアウト時に null bodyText を返す 🟡

**信頼性**: 🟡 *NFR-001から妥当な推測（WebViewはモック必要）*

**Given**: タイムアウト時間を極小（1ms）に設定した WebViewExtractor
**When**: `extract(url)` を呼ぶ
**Then**: `WebViewExtractionResult(bodyText = null)` を返す

**テストファイル**: `app/src/test/java/com/den4dr/share2Obsidian/util/WebViewExtractorTest.kt`

---

## 実装手順（TDD）

1. `/tsumiki:tdd-requirements` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 信頼性レベルサマリー

- 🔵 青信号: 1件（50%）
- 🟡 黄信号: 1件（50%）
- 🔴 赤信号: 0件（0%）

**品質評価**: 高品質
