# TASK-0007: HtmlToMarkdownConverter実装とテスト

**タスクID**: TASK-0007
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 3 - コンテンツ検出・テキスト処理
**信頼性レベル**: 🔵 *dataflow.md フロー3・REQ-202・ユーザヒアリング（Jsoup選定）より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/share-content-expansion/requirements.md) - REQ-202, REQ-203
- **設計文書**: [📐 architecture.md](../../design/share-content-expansion/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/share-content-expansion/dataflow.md)

## タスク概要

Jsoup を使用して HTML を Markdown に変換する `HtmlToMarkdownConverter` を TDD で実装する。h1/h2/h3、strong/b、em/i、a、li、p/br 要素を対応する Markdown 記法に変換する。

## 依存タスク

- **前提タスク**: [TASK-0001: 依存パッケージ追加](TASK-0001.md), [TASK-0002: データクラス定義](TASK-0002.md)
- **後続タスク**: [TASK-0008: HtmlContentProcessor実装](TASK-0008.md)

## 完了条件

- [ ] `util/HtmlToMarkdownConverter.kt` が実装されている
- [ ] h1 → `# テキスト`、h2 → `## テキスト`、h3 → `### テキスト` に変換される
- [ ] strong/b → `**テキスト**`、em/i → `*テキスト*` に変換される
- [ ] a → `[テキスト](href)` に変換される
- [ ] li → `- テキスト` に変換される
- [ ] p/br → 改行に変換される
- [ ] 単体テストカバレッジ 80%+ 達成
- [ ] `./gradlew testDebugUnitTest` が成功する

---

## 実装詳細

### 1. HtmlToMarkdownConverter クラス 🔵

**信頼性**: 🔵 *dataflow.md フロー3・ユーザヒアリング（Jsoup）より*

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/util/HtmlToMarkdownConverter.kt`

```kotlin
package com.den4dr.share2Obsidian.util

import org.jsoup.Jsoup
import org.jsoup.nodes.Element
import org.jsoup.nodes.Node
import org.jsoup.nodes.TextNode

object HtmlToMarkdownConverter {
    fun convert(html: String): String {
        val doc = Jsoup.parse(html)
        return convertNode(doc.body()).trim()
    }

    private fun convertNode(node: Node): String {
        return when (node) {
            is TextNode -> node.text()
            is Element -> convertElement(node)
            else -> ""
        }
    }

    private fun convertElement(element: Element): String {
        val childContent = element.childNodes().joinToString("") { convertNode(it) }
        return when (element.tagName()) {
            "h1" -> "# $childContent\n\n"
            "h2" -> "## $childContent\n\n"
            "h3" -> "### $childContent\n\n"
            "strong", "b" -> "**$childContent**"
            "em", "i" -> "*$childContent*"
            "a" -> "[$childContent](${element.attr("href")})"
            "li" -> "- $childContent\n"
            "ul", "ol" -> "$childContent\n"
            "p" -> "$childContent\n\n"
            "br" -> "\n"
            "body", "div", "span" -> childContent
            else -> childContent
        }
    }
}
```

---

## 単体テスト要件

### テストケース1: h1/h2/h3 変換 🔵

**信頼性**: 🔵 *dataflow.md フロー3より*

**Given**: html = `<h1>タイトル</h1>`
**When**: `HtmlToMarkdownConverter.convert(html)` を呼ぶ
**Then**: `# タイトル` を含む文字列を返す

**テストファイル**: `app/src/test/java/com/den4dr/share2Obsidian/util/HtmlToMarkdownConverterTest.kt`

### テストケース2: strong/b, em/i 変換 🔵

**Given**: html = `<p><strong>太字</strong>と<em>斜体</em></p>`
**When**: `HtmlToMarkdownConverter.convert(html)` を呼ぶ
**Then**: `**太字**と*斜体*` を含む文字列を返す

### テストケース3: a タグ変換 🔵

**Given**: html = `<a href="https://example.com">リンク</a>`
**When**: `HtmlToMarkdownConverter.convert(html)` を呼ぶ
**Then**: `[リンク](https://example.com)` を含む文字列を返す

### テストケース4: li タグ変換 🔵

**Given**: html = `<ul><li>項目1</li><li>項目2</li></ul>`
**When**: `HtmlToMarkdownConverter.convert(html)` を呼ぶ
**Then**: `- 項目1` と `- 項目2` を含む文字列を返す

### テストケース5: 空文字 HTML 🟡

**Given**: html = ``
**When**: `HtmlToMarkdownConverter.convert(html)` を呼ぶ
**Then**: 空文字を返す（クラッシュしない）

---

## 実装手順（TDD）

1. `/tsumiki:tdd-requirements` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 信頼性レベルサマリー

- 🔵 青信号: 4件（80%）
- 🟡 黄信号: 1件（20%）
- 🔴 赤信号: 0件（0%）

**品質評価**: 高品質
