# TASK-0005: ContentTypeDetector実装とテスト

**タスクID**: TASK-0005
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 3 - コンテンツ検出・テキスト処理
**信頼性レベル**: 🔵 *architecture.md コンテンツ処理層・REQ-001/101/201/301より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/share-content-expansion/requirements.md) - REQ-001, REQ-101, REQ-201, REQ-301
- **設計文書**: [📐 architecture.md](../../design/share-content-expansion/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/share-content-expansion/dataflow.md)

## タスク概要

`Intent` から MIME タイプ・EXTRA_TEXT・EXTRA_HTML_TEXT・EXTRA_STREAM を読み取り、適切な `ShareContent` sealed class インスタンスを返す `ContentTypeDetector` を TDD で実装する。URL 判定には正規表現を使用する。

## 依存タスク

- **前提タスク**: [TASK-0002: ShareContentデータクラス・インターフェース定義](TASK-0002.md)
- **後続タスク**: [TASK-0013: MainActivity統合リファクタリング](TASK-0013.md)

## 完了条件

- [ ] `content/ContentTypeDetector.kt` が実装されている
- [ ] `text/plain` + URL パターン → `ShareContent.Url` を返す
- [ ] `text/plain` + 非URL → `ShareContent.Text` を返す
- [ ] `text/html` → `ShareContent.Html` を返す（EXTRA_HTML_TEXT と EXTRA_TEXT 両方取得）
- [ ] `image/*` / `application/*` → `ShareContent.File` を返す
- [ ] EXTRA_SUBJECT が存在する場合 title に設定される
- [ ] 単体テストカバレッジ 80%+ 達成

---

## 実装詳細

### 1. ContentTypeDetector クラス 🔵

**信頼性**: 🔵 *architecture.md・dataflow.md・REQ-001/101/201/301より*

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/content/ContentTypeDetector.kt`

```kotlin
package com.den4dr.share2Obsidian.content

import android.content.Intent
import android.net.Uri

object ContentTypeDetector {
    private val URL_PATTERN = Regex("^https?://[^\\s]+$")

    fun detect(intent: Intent): ShareContent? {
        val mimeType = intent.type ?: return null
        val title = intent.getStringExtra(Intent.EXTRA_SUBJECT)

        return when {
            mimeType == "text/html" -> {
                val html = intent.getStringExtra("android.intent.extra.HTML_TEXT")
                val fallback = intent.getStringExtra(Intent.EXTRA_TEXT) ?: ""
                ShareContent.Html(html = html, fallbackText = fallback, title = title)
            }
            mimeType == "text/plain" -> {
                val text = intent.getStringExtra(Intent.EXTRA_TEXT) ?: return null
                if (URL_PATTERN.matches(text.trim())) {
                    ShareContent.Url(url = text.trim(), title = title)
                } else {
                    ShareContent.Text(text = text, title = title)
                }
            }
            mimeType.startsWith("image/") || mimeType.startsWith("application/") -> {
                val uri = intent.getParcelableExtra<Uri>(Intent.EXTRA_STREAM) ?: return null
                ShareContent.File(uri = uri, title = title)
            }
            else -> null
        }
    }
}
```

---

## 単体テスト要件

### テストケース1: text/plain + URL → ShareContent.Url 🔵

**信頼性**: 🔵 *REQ-101・dataflow.mdより*

**Given**: MIME=text/plain, EXTRA_TEXT="https://example.com"
**When**: `ContentTypeDetector.detect(intent)` を呼ぶ
**Then**: `ShareContent.Url(url="https://example.com")` が返る

**テストファイル**: `app/src/test/java/com/den4dr/share2Obsidian/content/ContentTypeDetectorTest.kt`

### テストケース2: text/plain + 非URL → ShareContent.Text 🔵

**Given**: MIME=text/plain, EXTRA_TEXT="普通のテキスト"
**When**: `ContentTypeDetector.detect(intent)` を呼ぶ
**Then**: `ShareContent.Text(text="普通のテキスト")` が返る

### テストケース3: text/html → ShareContent.Html 🔵

**Given**: MIME=text/html, EXTRA_HTML_TEXT="<b>Bold</b>", EXTRA_TEXT="Bold"
**When**: `ContentTypeDetector.detect(intent)` を呼ぶ
**Then**: `ShareContent.Html(html="<b>Bold</b>", fallbackText="Bold")` が返る

### テストケース4: image/* → ShareContent.File 🔵

**Given**: MIME=image/png, EXTRA_STREAM=Uri("content://...")
**When**: `ContentTypeDetector.detect(intent)` を呼ぶ
**Then**: `ShareContent.File(uri=...)` が返る

### テストケース5: EXTRA_SUBJECT が title に設定される 🔵

**Given**: MIME=text/plain, EXTRA_TEXT="本文", EXTRA_SUBJECT="タイトル"
**When**: `ContentTypeDetector.detect(intent)` を呼ぶ
**Then**: 返り値の title が "タイトル"

---

## 実装手順（TDD）

1. `/tsumiki:tdd-requirements` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 信頼性レベルサマリー

- 🔵 青信号: 5件（100%）
- 🟡 黄信号: 0件（0%）
- 🔴 赤信号: 0件（0%）

**品質評価**: 高品質
