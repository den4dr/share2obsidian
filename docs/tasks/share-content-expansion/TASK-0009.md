# TASK-0009: FileContentProcessor実装とテスト

**タスクID**: TASK-0009
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 4 - HTML・ファイル処理
**信頼性レベル**: 🔵 *dataflow.md フロー4・REQ-301/302より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/share-content-expansion/requirements.md) - REQ-301, REQ-302, REQ-303
- **設計文書**: [📐 architecture.md](../../design/share-content-expansion/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/share-content-expansion/dataflow.md)

## タスク概要

`ShareContent.File` を受け取り、ファイル URI をクリップボードにコピーする `FileContentProcessor` を TDD で実装する。`ClipboardManager.setPrimaryClip()` を使用する。

## 依存タスク

- **前提タスク**: [TASK-0002: データクラス定義](TASK-0002.md)
- **後続タスク**: [TASK-0013: MainActivity統合リファクタリング](TASK-0013.md)

## 完了条件

- [ ] `content/FileContentProcessor.kt` が実装されている
- [ ] `ContentProcessor<ShareContent.File>` を implements している
- [ ] `ClipboardManager.setPrimaryClip(ClipData.newUri(...))` が呼ばれる
- [ ] body に「クリップボードに画像をコピーしました」等のガイダンステキストが設定される
- [ ] contentType = ContentKind.FILE が設定される
- [ ] 単体テストカバレッジ 80%+ 達成（ClipboardManager はモック）

---

## 実装詳細

### 1. FileContentProcessor クラス 🔵

**信頼性**: 🔵 *dataflow.md フロー4・REQ-301/302より*

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/content/FileContentProcessor.kt`

```kotlin
package com.den4dr.share2Obsidian.content

import android.content.ClipData
import android.content.ClipboardManager
import android.content.Context

class FileContentProcessor(
    private val context: Context
) : ContentProcessor<ShareContent.File> {
    override suspend fun process(content: ShareContent.File): ProcessedContent {
        val clipboard = context.getSystemService(Context.CLIPBOARD_SERVICE) as ClipboardManager
        val clip = ClipData.newUri(
            context.contentResolver,
            "Shared File",
            content.uri
        )
        clipboard.setPrimaryClip(clip)

        return ProcessedContent(
            body = "(クリップボードに画像をコピーしました。Obsidian 上で貼り付けてください。)",
            title = content.title,
            contentType = ContentKind.FILE
        )
    }
}
```

---

## 単体テスト要件

### テストケース1: ClipboardManager にファイル URI がセットされる 🔵

**信頼性**: 🔵 *REQ-302・dataflow.md フロー4より*

**Given**: ShareContent.File(uri=testUri), モック ClipboardManager, モック Context
**When**: `FileContentProcessor(context).process(content)` を呼ぶ
**Then**: `clipboard.setPrimaryClip()` が1回呼ばれている

**テストファイル**: `app/src/test/java/com/den4dr/share2Obsidian/content/FileContentProcessorTest.kt`

### テストケース2: body にガイダンステキストが設定される 🔵

**Given**: ShareContent.File(uri=testUri)
**When**: `FileContentProcessor(context).process(content)` を呼ぶ
**Then**: body が空でない ProcessedContent を返す

### テストケース3: contentType が FILE 🟡

**Given**: 任意の ShareContent.File
**When**: `FileContentProcessor(context).process(content)` を呼ぶ
**Then**: contentType = ContentKind.FILE

---

## 実装手順（TDD）

1. `/tsumiki:tdd-requirements` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 信頼性レベルサマリー

- 🔵 青信号: 3件（75%）
- 🟡 黄信号: 1件（25%）
- 🔴 赤信号: 0件（0%）

**品質評価**: 高品質
