# TASK-0008: HtmlContentProcessor実装とテスト

**タスクID**: TASK-0008
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 4 - HTML・ファイル処理
**信頼性レベル**: 🔵 *dataflow.md フロー3・REQ-201/202/203より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/share-content-expansion/requirements.md) - REQ-201, REQ-202, REQ-203
- **設計文書**: [📐 architecture.md](../../design/share-content-expansion/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/share-content-expansion/dataflow.md)

## タスク概要

`ShareContent.Html` を受け取り、`HtmlToMarkdownConverter` を使って Markdown に変換する `HtmlContentProcessor` を TDD で実装する。EXTRA_HTML_TEXT が null のとき fallbackText を使用する。

## 依存タスク

- **前提タスク**: [TASK-0007: HtmlToMarkdownConverter実装](TASK-0007.md)
- **後続タスク**: [TASK-0013: MainActivity統合リファクタリング](TASK-0013.md)

## 完了条件

- [ ] `content/HtmlContentProcessor.kt` が実装されている
- [ ] `ContentProcessor<ShareContent.Html>` を implements している
- [ ] html が非null のとき `HtmlToMarkdownConverter.convert(html)` を呼ぶ
- [ ] html が null のとき fallbackText をそのまま body にする
- [ ] 変換例外時は fallbackText にフォールバックする
- [ ] contentType = ContentKind.HTML が設定される
- [ ] 単体テストカバレッジ 80%+ 達成

---

## 実装詳細

### 1. HtmlContentProcessor クラス 🔵

**信頼性**: 🔵 *dataflow.md フロー3・REQ-201/202/203より*

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/content/HtmlContentProcessor.kt`

```kotlin
package com.den4dr.share2Obsidian.content

import com.den4dr.share2Obsidian.util.HtmlToMarkdownConverter

class HtmlContentProcessor : ContentProcessor<ShareContent.Html> {
    override suspend fun process(content: ShareContent.Html): ProcessedContent {
        val body = if (content.html != null) {
            try {
                HtmlToMarkdownConverter.convert(content.html)
            } catch (e: Exception) {
                content.fallbackText
            }
        } else {
            content.fallbackText
        }
        return ProcessedContent(
            body = body,
            title = content.title,
            contentType = ContentKind.HTML
        )
    }
}
```

---

## 単体テスト要件

### テストケース1: HTML が Markdown に変換される 🔵

**信頼性**: 🔵 *REQ-201・dataflow.md フロー3より*

**Given**: ShareContent.Html(html="<h1>タイトル</h1>", fallbackText="タイトル")
**When**: `HtmlContentProcessor().process(content)` を呼ぶ
**Then**: body が `# タイトル` を含む ProcessedContent を返す

**テストファイル**: `app/src/test/java/com/den4dr/share2Obsidian/content/HtmlContentProcessorTest.kt`

### テストケース2: html が null のとき fallbackText を使用 🔵

**信頼性**: 🔵 *REQ-203より*

**Given**: ShareContent.Html(html=null, fallbackText="フォールバックテキスト")
**When**: `HtmlContentProcessor().process(content)` を呼ぶ
**Then**: body = "フォールバックテキスト" の ProcessedContent を返す

### テストケース3: contentType が HTML に設定される 🟡

**Given**: 任意の ShareContent.Html
**When**: `HtmlContentProcessor().process(content)` を呼ぶ
**Then**: contentType = ContentKind.HTML

---

## 実装手順（TDD）

1. `/tsumiki:tdd-requirements` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 信頼性レベルサマリー

- 🔵 青信号: 3件（75%）
- 🟡 黄信号: 1件（25%）
- 🔴 赤信号: 0件（0%）

**品質評価**: 高品質
