# TASK-0003: FrontmatterBuilder実装とテスト

**タスクID**: TASK-0003
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 2 - フォーマット層実装
**信頼性レベル**: 🔵 *dataflow.md Frontmatter生成フロー・REQ-003より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/share-content-expansion/requirements.md) - REQ-003
- **設計文書**: [📐 architecture.md](../../design/share-content-expansion/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/share-content-expansion/dataflow.md)

## タスク概要

YAML Frontmatter ヘッダーを生成する `FrontmatterBuilder` クラスを TDD で実装する。title あり/なし の両パターン、固定 tags:[shared] を含む Frontmatter 文字列を返す。

## 依存タスク

- **前提タスク**: [TASK-0002: ShareContentデータクラス・インターフェース定義](TASK-0002.md)
- **後続タスク**: [TASK-0013: MainActivity統合リファクタリング](TASK-0013.md)

## 完了条件

- [ ] `format/FrontmatterBuilder.kt` が実装されている
- [ ] title が非null のとき `---\ntitle: "..."\ntags: [shared]\n---\n\n本文` を返す
- [ ] title が null のとき `---\ntags: [shared]\n---\n\n本文` を返す
- [ ] 単体テストカバレッジ 80%+ 達成
- [ ] `./gradlew testDebugUnitTest` が成功する

---

## 実装詳細

### 1. FrontmatterBuilder クラス 🔵

**信頼性**: 🔵 *dataflow.md Frontmatter生成フロー・REQ-003より*

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/format/FrontmatterBuilder.kt`

```kotlin
package com.den4dr.share2Obsidian.format

import com.den4dr.share2Obsidian.AppConfig

object FrontmatterBuilder {
    fun build(title: String?, body: String): String {
        val tags = AppConfig.OBSIDIAN_TAGS.joinToString(", ")
        val frontmatter = if (title != null) {
            "---\ntitle: \"$title\"\ntags: [$tags]\n---\n\n$body"
        } else {
            "---\ntags: [$tags]\n---\n\n$body"
        }
        return frontmatter
    }
}
```

---

## 単体テスト要件

### テストケース1: title あり の Frontmatter 生成 🔵

**信頼性**: 🔵 *dataflow.md Frontmatter生成フロー・REQ-003より*

**Given**: title = "テスト", body = "本文テキスト"
**When**: `FrontmatterBuilder.build(title, body)` を呼ぶ
**Then**: `---\ntitle: "テスト"\ntags: [shared]\n---\n\n本文テキスト` を返す

**テストファイル**: `app/src/test/java/com/den4dr/share2Obsidian/format/FrontmatterBuilderTest.kt`

### テストケース2: title が null の Frontmatter 生成 🔵

**信頼性**: 🔵 *dataflow.md Frontmatter生成フロー・REQ-003より*

**Given**: title = null, body = "本文テキスト"
**When**: `FrontmatterBuilder.build(null, body)` を呼ぶ
**Then**: `---\ntags: [shared]\n---\n\n本文テキスト` を返す

### テストケース3: 空文字 body 🟡

**信頼性**: 🟡 *エッジケースとして妥当な推測*

**Given**: title = null, body = ""
**When**: `FrontmatterBuilder.build(null, "")` を呼ぶ
**Then**: `---\ntags: [shared]\n---\n\n` を返す（クラッシュしない）

---

## 実装手順（TDD）

1. `/tsumiki:tdd-requirements` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 信頼性レベルサマリー

- 🔵 青信号: 3件（75%）
- 🟡 黄信号: 1件（25%）
- 🔴 赤信号: 0件（0%）

**品質評価**: 高品質
