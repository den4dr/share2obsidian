# TASK-0011: UrlContentProcessor実装とテスト

**タスクID**: TASK-0011
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 5 - URL処理・UI実装
**信頼性レベル**: 🔵 *dataflow.md フロー2・REQ-101/102/103より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/share-content-expansion/requirements.md) - REQ-101, REQ-102, REQ-103
- **設計文書**: [📐 architecture.md](../../design/share-content-expansion/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/share-content-expansion/dataflow.md)

## タスク概要

`ShareContent.Url` を受け取り、`WebViewExtractor` を使って本文を抽出する `UrlContentProcessor` を TDD で実装する。抽出失敗（null）時は URL テキストをそのまま body にフォールバックする。

## 依存タスク

- **前提タスク**: [TASK-0010: WebViewExtractor実装](TASK-0010.md)
- **後続タスク**: [TASK-0013: MainActivity統合リファクタリング](TASK-0013.md)

## 完了条件

- [ ] `content/UrlContentProcessor.kt` が実装されている
- [ ] `ContentProcessor<ShareContent.Url>` を implements している
- [ ] `WebViewExtractor.extract(url)` を呼んでいる
- [ ] bodyText が null でない場合 bodyText を body にする
- [ ] bodyText が null の場合 url をそのまま body にする（フォールバック）
- [ ] contentType = ContentKind.URL が設定される
- [ ] 単体テストカバレッジ 80%+ 達成（WebViewExtractor はモック）

---

## 実装詳細

### 1. UrlContentProcessor クラス 🔵

**信頼性**: 🔵 *dataflow.md フロー2・REQ-101/102/103より*

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/content/UrlContentProcessor.kt`

```kotlin
package com.den4dr.share2Obsidian.content

import com.den4dr.share2Obsidian.util.WebViewExtractor

class UrlContentProcessor(
    private val webViewExtractor: WebViewExtractor
) : ContentProcessor<ShareContent.Url> {
    override suspend fun process(content: ShareContent.Url): ProcessedContent {
        val result = webViewExtractor.extract(content.url)
        val body = result.bodyText ?: content.url  // フォールバック: URL そのまま
        return ProcessedContent(
            body = body,
            title = content.title,
            contentType = ContentKind.URL
        )
    }
}
```

---

## 単体テスト要件

### テストケース1: WebView 抽出成功 → bodyText が body になる 🔵

**信頼性**: 🔵 *REQ-102・dataflow.md フロー2より*

**Given**: WebViewExtractor モックが bodyText="ページ本文" を返す
**When**: `UrlContentProcessor(mockExtractor).process(content)` を呼ぶ
**Then**: ProcessedContent(body="ページ本文", contentType=URL)

**テストファイル**: `app/src/test/java/com/den4dr/share2Obsidian/content/UrlContentProcessorTest.kt`

### テストケース2: bodyText が null → URL フォールバック 🔵

**信頼性**: 🔵 *REQ-103・dataflow.md フロー2より*

**Given**: WebViewExtractor モックが bodyText=null を返す, url="https://example.com"
**When**: `UrlContentProcessor(mockExtractor).process(content)` を呼ぶ
**Then**: ProcessedContent(body="https://example.com", contentType=URL)

### テストケース3: contentType が URL 🟡

**Given**: 任意の ShareContent.Url
**When**: `UrlContentProcessor(mockExtractor).process(content)` を呼ぶ
**Then**: contentType = ContentKind.URL

---

## 実装手順（TDD）

1. `/tsumiki:tdd-requirements` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 信頼性レベルサマリー

- 🔵 青信号: 3件（75%）
- 🟡 黄信号: 1件（25%）
- 🔴 赤信号: 0件（0%）

**品質評価**: 高品質
