# TASK-0012: LoadingScreen UI実装とテスト

**タスクID**: TASK-0012
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 5 - URL処理・UI実装
**信頼性レベル**: 🔵 *ユーザヒアリング（プログレスバーありActivity）・architecture.mdより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/share-content-expansion/requirements.md)
- **設計文書**: [📐 architecture.md](../../design/share-content-expansion/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/share-content-expansion/dataflow.md)

## タスク概要

URL コンテンツ取得中に表示する Jetpack Compose プログレスバー画面 `LoadingScreen` を実装する。`CircularProgressIndicator` を使用し、「URL の内容を取得中...」等のメッセージを表示する。

## 依存タスク

- **前提タスク**: [TASK-0002: データクラス定義](TASK-0002.md)
- **後続タスク**: [TASK-0013: MainActivity統合リファクタリング](TASK-0013.md)

## 完了条件

- [ ] `ui/LoadingScreen.kt` が実装されている
- [ ] Compose `@Composable` 関数 `LoadingScreen()` が定義されている
- [ ] `CircularProgressIndicator` が表示されている
- [ ] ローディングメッセージ文字列が表示されている
- [ ] Compose プレビュー `@Preview` が追加されている
- [ ] 単体テストカバレッジ 80%+ 達成（Compose テスト）

---

## 実装詳細

### 1. LoadingScreen Composable 🔵

**信頼性**: 🔵 *ユーザヒアリング（プログレスバーあり）・architecture.mdより*

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/ui/LoadingScreen.kt`

```kotlin
package com.den4dr.share2Obsidian.ui

import androidx.compose.foundation.layout.*
import androidx.compose.material3.CircularProgressIndicator
import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import androidx.compose.ui.Alignment
import androidx.compose.ui.Modifier
import androidx.compose.ui.tooling.preview.Preview
import androidx.compose.ui.unit.dp

@Composable
fun LoadingScreen() {
    Column(
        modifier = Modifier.fillMaxSize(),
        horizontalAlignment = Alignment.CenterHorizontally,
        verticalArrangement = Arrangement.Center
    ) {
        CircularProgressIndicator()
        Spacer(modifier = Modifier.height(16.dp))
        Text(text = "URLの内容を取得中...")
    }
}

@Preview(showBackground = true)
@Composable
fun LoadingScreenPreview() {
    LoadingScreen()
}
```

---

## UI/UX要件

### ローディング状態 🔵

**信頼性**: 🔵 *ユーザヒアリング（プログレスバーありActivity）より*

- `CircularProgressIndicator` を画面中央に表示
- 「URLの内容を取得中...」テキストをインジケータの下に表示
- Activity の背景は透過またはシステムデフォルト

---

## 単体テスト要件

### テストケース1: LoadingScreen が表示される 🟡

**信頼性**: 🟡 *Compose テストから妥当な推測*

**Given**: LoadingScreen Composable
**When**: Compose テストルールで描画する
**Then**: CircularProgressIndicator が表示されている

**テストファイル**: `app/src/androidTest/java/com/den4dr/share2Obsidian/ui/LoadingScreenTest.kt`
（Compose テストは Instrumentation テストとして実施）

---

## 実装手順（TDD）

1. `/tsumiki:tdd-requirements` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 信頼性レベルサマリー

- 🔵 青信号: 1件（50%）
- 🟡 黄信号: 1件（50%）
- 🔴 赤信号: 0件（0%）

**品質評価**: 高品質
