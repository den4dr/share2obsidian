# TASK-0013: MainActivity統合リファクタリング

**タスクID**: TASK-0013
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 6 - 統合・仕上げ
**信頼性レベル**: 🔵 *architecture.md・dataflow.md 全体フロー・REQ-001〜401より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/share-content-expansion/requirements.md)
- **設計文書**: [📐 architecture.md](../../design/share-content-expansion/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/share-content-expansion/dataflow.md)

## タスク概要

既存の 26 行 MainActivity を、全 Processor クラスを統合した完全な実装にリファクタリングする。`ContentTypeDetector` → `when` 式で適切な Processor を選択 → `FrontmatterBuilder` → `ObsidianUriBuilder` → `startActivity` → `finish()` のフローを実装する。URL 共有時は `LoadingScreen` を表示し `lifecycleScope.launch` で非同期処理する。

## 依存タスク

- **前提タスク**: TASK-0003, TASK-0004, TASK-0005, TASK-0006, TASK-0008, TASK-0009, TASK-0011, TASK-0012
- **後続タスク**: [TASK-0014: strings.xmlとエラーハンドリング](TASK-0014.md)

## 完了条件

- [ ] 既存の MainActivity が新しいアーキテクチャで書き直されている
- [ ] `ContentTypeDetector.detect(intent)` を `onCreate` で呼んでいる
- [ ] `when (shareContent)` で各 Processor を選択している
- [ ] URL 共有時は `setContent { LoadingScreen() }` → `lifecycleScope.launch` を使用している
- [ ] `FrontmatterBuilder.build()` で Frontmatter を付与している
- [ ] `ObsidianUriBuilder.build()` で Obsidian URI を生成している
- [ ] `startActivity(Intent(ACTION_VIEW, uri))` で Obsidian を起動している
- [ ] `finish()` でアプリを終了している
- [ ] `ActivityNotFoundException` を catch して Toast を表示している
- [ ] 単体テストカバレッジ 80%+ 達成

---

## 実装詳細

### 1. MainActivity リファクタリング 🔵

**信頼性**: 🔵 *dataflow.md システム全体フロー・REQ-001〜401より*

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/MainActivity.kt`

```kotlin
package com.den4dr.share2Obsidian

import android.content.ActivityNotFoundException
import android.content.Intent
import android.os.Bundle
import android.widget.Toast
import androidx.activity.ComponentActivity
import androidx.activity.compose.setContent
import androidx.lifecycle.lifecycleScope
import com.den4dr.share2Obsidian.content.*
import com.den4dr.share2Obsidian.format.FrontmatterBuilder
import com.den4dr.share2Obsidian.format.ObsidianUriBuilder
import com.den4dr.share2Obsidian.ui.LoadingScreen
import com.den4dr.share2Obsidian.util.WebViewExtractor
import kotlinx.coroutines.launch

class MainActivity : ComponentActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)

        val shareContent = ContentTypeDetector.detect(intent)
        if (shareContent == null) {
            finish()
            return
        }

        if (shareContent is ShareContent.Url) {
            setContent { LoadingScreen() }
        }

        lifecycleScope.launch {
            val processed = when (shareContent) {
                is ShareContent.Text -> TextContentProcessor().process(shareContent)
                is ShareContent.Url -> UrlContentProcessor(WebViewExtractor(this@MainActivity)).process(shareContent)
                is ShareContent.Html -> HtmlContentProcessor().process(shareContent)
                is ShareContent.File -> FileContentProcessor(this@MainActivity).process(shareContent)
            }

            val content = FrontmatterBuilder.build(processed.title, processed.body)
            val uri = ObsidianUriBuilder.build(content, processed.title)

            try {
                startActivity(Intent(Intent.ACTION_VIEW, uri))
            } catch (e: ActivityNotFoundException) {
                Toast.makeText(
                    this@MainActivity,
                    getString(R.string.error_obsidian_not_installed),
                    Toast.LENGTH_LONG
                ).show()
            }
            finish()
        }
    }
}
```

---

## 単体テスト要件

### テストケース1: text/plain Intent が処理される 🔵

**信頼性**: 🔵 *REQ-001・dataflow.md フロー1より*

**Given**: MIME=text/plain, EXTRA_TEXT="テキスト" のモック Intent
**When**: MainActivity が起動する
**Then**: `startActivity` に obsidian://new URI が渡される

**テストファイル**: `app/src/test/java/com/den4dr/share2Obsidian/MainActivityTest.kt`

### テストケース2: Obsidian 未インストール時 Toast 表示 🔵

**信頼性**: 🔵 *REQ-401/402より*

**Given**: `startActivity` が `ActivityNotFoundException` をスローするモック環境
**When**: MainActivity が起動して処理が完了する
**Then**: Toast が表示され、`finish()` が呼ばれる

---

## 実装手順（TDD）

1. `/tsumiki:tdd-requirements` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 信頼性レベルサマリー

- 🔵 青信号: 3件（75%）
- 🟡 黄信号: 1件（25%）
- 🔴 赤信号: 0件（0%）

**品質評価**: 高品質
