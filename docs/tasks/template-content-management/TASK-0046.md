# TASK-0046: DataStore NoteSettings・Repository・Hilt Module 実装

**タスクID**: TASK-0046
**タスクタイプ**: TDD
**推定工数**: 8時間
**フェーズ**: Phase 1 - ドメイン・永続化基盤
**信頼性レベル**: 🔵 *REQ-021・interfaces.kt NoteSettings/NoteSettingsRepository・architecture.md DataStore 設計より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/template-content-management/requirements.md) - REQ-021
- **設計文書**: [📐 architecture.md](../../design/template-content-management/architecture.md) - DataStore 設計
- **型定義**: [📝 interfaces.kt](../../design/template-content-management/interfaces.kt)
- **データフロー**: [🔄 dataflow.md](../../design/template-content-management/dataflow.md) - フロー3, フロー6

## タスク概要

vault/folder をアプリ全体のグローバルデフォルト設定として保持する DataStore Preferences 層を新規実装する。`NoteSettings` データクラス、`NoteSettingsRepository` インターフェース、`NoteSettingsRepositoryImpl`（DataStore 実装）、Hilt の `DataStoreModule` を追加する。

## 依存タスク

- **前提タスク**: [TASK-0043](TASK-0043.md)（datastore-preferences 依存）
- **後続タスク**: [TASK-0047](TASK-0047.md), [TASK-0052](TASK-0052.md), [TASK-0053](TASK-0053.md)

## 完了条件

- [ ] `NoteSettings(vault="", folder="")` データクラスが追加されている
- [ ] `NoteSettingsRepository` インターフェース（getSettings/saveVault/saveFolder）が定義されている
- [ ] `NoteSettingsRepositoryImpl` が DataStore Preferences で読み書きを実装している
- [ ] `DataStoreModule`（Hilt）が `NoteSettingsRepository` を Singleton で提供する
- [ ] 未設定時に vault/folder が空文字列で返る（デフォルト値）
- [ ] Repository の読み書きテストが成功する

---

## 実装詳細

### 1. NoteSettings データクラス 🔵

**信頼性**: 🔵 *interfaces.kt NoteSettings 定義より*

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/data/datastore/NoteSettings.kt`

```kotlin
data class NoteSettings(
    val vault: String = "",
    val folder: String = "",
)
```

### 2. NoteSettingsRepository インターフェース 🔵

**信頼性**: 🔵 *interfaces.kt NoteSettingsRepository 定義より*

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/data/datastore/NoteSettingsRepository.kt`

```kotlin
interface NoteSettingsRepository {
    fun getSettings(): Flow<NoteSettings>
    suspend fun saveVault(vault: String)
    suspend fun saveFolder(folder: String)
}
```

### 3. NoteSettingsRepositoryImpl 🔵

**信頼性**: 🔵 *interfaces.kt・DataStore Preferences 公式パターンより*

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/data/datastore/NoteSettingsRepositoryImpl.kt`

```kotlin
val VAULT_KEY = stringPreferencesKey("vault")
val FOLDER_KEY = stringPreferencesKey("folder")

val Context.noteSettingsDataStore: DataStore<Preferences>
    by preferencesDataStore(name = "note_settings")

class NoteSettingsRepositoryImpl(
    private val dataStore: DataStore<Preferences>,
) : NoteSettingsRepository {
    override fun getSettings(): Flow<NoteSettings> = dataStore.data.map { prefs ->
        NoteSettings(
            vault = prefs[VAULT_KEY] ?: "",
            folder = prefs[FOLDER_KEY] ?: "",
        )
    }
    override suspend fun saveVault(vault: String) {
        dataStore.edit { it[VAULT_KEY] = vault }
    }
    override suspend fun saveFolder(folder: String) {
        dataStore.edit { it[FOLDER_KEY] = folder }
    }
}
```

### 4. DataStoreModule（Hilt） 🔵

**信頼性**: 🔵 *interfaces.kt DataStoreModule・Hilt + DataStore 公式パターンより*

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/di/DataStoreModule.kt`

```kotlin
@Module
@InstallIn(SingletonComponent::class)
object DataStoreModule {
    @Provides
    @Singleton
    fun provideNoteSettingsRepository(
        @ApplicationContext context: Context,
    ): NoteSettingsRepository = NoteSettingsRepositoryImpl(context.noteSettingsDataStore)
}
```

---

## 単体テスト要件

### テストケース1: 保存した vault/folder を読み出せる 🔵

**信頼性**: 🔵 *REQ-021・Repository 読み書き要件より*

**Given**: テスト用 DataStore を持つ `NoteSettingsRepositoryImpl`
**When**: `saveVault("MyVault")` → `getSettings().first()`
**Then**: `vault == "MyVault"`

**テストファイル**: `app/src/test/java/com/den4dr/share2Obsidian/data/datastore/NoteSettingsRepositoryImplTest.kt`

> テスト用 DataStore は `PreferenceDataStoreFactory.create` を一時ディレクトリに作成して使用（Robolectric 環境で実行可能）。

### テストケース2: 未設定時は空文字列が返る 🔵

**信頼性**: 🔵 *architecture.md デフォルト値・dataflow.md フロー6 より*

**Given**: 何も保存していない DataStore
**When**: `getSettings().first()`
**Then**: `vault == "" && folder == ""`

---

## 統合テスト要件

### 統合テスト1: Hilt による NoteSettingsRepository 注入 🟡

**信頼性**: 🟡 *Hilt DI の標準動作から妥当な推測*

**テスト内容**: `DataStoreModule` 経由で `NoteSettingsRepository` が単一インスタンスとして提供される
**期待結果**: 注入が成功し getSettings が動作する（後続の SettingsViewModel/MainActivity 統合タスクで実機確認）

---

## UI/UX要件

該当なし（データ層）。

---

## 実装手順

### TDDタスクの場合

1. `/tsumiki:tdd-requirements TASK-0046` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- DataStore の読み書きは IO Dispatcher で実行される設計（architecture.md パフォーマンス）
- DataStore 名は `note_settings`（architecture.md より）
- vault/folder のデフォルトは空文字列（`AppConfig.OBSIDIAN_VAULT` のテスト用ハードコードは引き継がない）

---

## 信頼性レベルサマリー

### 項目別信頼性

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 4 | 0 | 0 | 4 |
| 単体テスト | 2 | 0 | 0 | 2 |
| 統合テスト | 0 | 1 | 0 | 1 |

### 全体評価

- **総項目数**: 7項目
- 🔵 **青信号**: 6項目 (86%)
- 🟡 **黄信号**: 1項目 (14%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質
