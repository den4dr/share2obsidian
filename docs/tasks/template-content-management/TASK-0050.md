# TASK-0050: TemplateEditViewModel・TemplateEditUiState body 対応

**タスクID**: TASK-0050
**タスクタイプ**: TDD
**推定工数**: 6時間
**フェーズ**: Phase 4 - テンプレート管理・設定 UI
**信頼性レベル**: 🔵 *REQ-051, REQ-052・interfaces.kt TemplateEditUiState/TemplateEditViewModel より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/template-content-management/requirements.md) - REQ-051, REQ-052
- **設計文書**: [📐 architecture.md](../../design/template-content-management/architecture.md) - ui/template/TemplateEditViewModel.kt
- **型定義**: [📝 interfaces.kt](../../design/template-content-management/interfaces.kt)
- **データフロー**: [🔄 dataflow.md](../../design/template-content-management/dataflow.md) - フロー4
- **受け入れ基準**: [✅ acceptance-criteria.md](../../spec/template-content-management/acceptance-criteria.md) - TC-051-04, TC-051-05

## タスク概要

`TemplateEditUiState` から vault/folder を削除し本文テンプレート `body` を追加する。`TemplateEditViewModel` の `updateVault/updateFolder` を削除し `updateBody` を追加、`save()` の Template 構築から vault/folder を削除して body を含める。

## 依存タスク

- **前提タスク**: [TASK-0044](TASK-0044.md)（Template.body）
- **後続タスク**: [TASK-0051](TASK-0051.md)

## 完了条件

- [ ] `TemplateEditUiState` から vault/folder が削除され `body: String = ""` が追加されている
- [ ] `updateVault()`/`updateFolder()` が削除されている
- [ ] `updateBody(body: String)` が追加されている
- [ ] `save()` が `Template(..., body = state.body, ...)` を構築し vault/folder を含まない
- [ ] 編集モードで既存テンプレートの body が読み込まれる
- [ ] 変更後の `TemplateEditViewModelTest` が成功する

---

## 実装詳細

### 1. TemplateEditUiState の変更 🔵

**信頼性**: 🔵 *interfaces.kt TemplateEditUiState 定義より*

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/ui/template/TemplateEditViewModel.kt`（同ファイル内 UiState）

```kotlin
data class TemplateEditUiState(
    val templateId: Long? = null,
    val name: String = "",
    val body: String = "",           // 新規追加
    val isDefault: Boolean = false,
    val fields: List<TemplateFieldEditState> = emptyList(),
    val isSaving: Boolean = false,
    val errorMessage: String? = null,
    val isSaved: Boolean = false,
    // vault, folder を削除
)
```

### 2. updateBody 追加・updateVault/Folder 削除 🔵

**信頼性**: 🔵 *interfaces.kt TemplateEditViewModel より*

```kotlin
fun updateBody(body: String) = _uiState.update { it.copy(body = body) }
// updateVault(), updateFolder() を削除
```

### 3. save() の Template 構築修正 🔵

**信頼性**: 🔵 *interfaces.kt save()・dataflow.md フロー4 より*

```kotlin
val template = Template(
    id = state.templateId ?: 0L,
    name = state.name,
    body = state.body,           // 新規
    isDefault = state.isDefault,
    fields = state.fields.mapIndexed { /* 既存 */ },
    // vault, folder は含まない
)
```

### 4. 編集モードの body 読み込み 🔵

**信頼性**: 🔵 *既存 TemplateEditViewModel の編集ロード処理より*

- 既存テンプレート読み込み時に `body = template.body` を UiState に反映する

---

## 単体テスト要件

### テストケース1: updateBody で body が更新される 🔵

**信頼性**: 🔵 *interfaces.kt より*

**Given**: 初期 UiState
**When**: `updateBody("## メモ\n{{content}}")`
**Then**: `uiState.value.body == "## メモ\n{{content}}"`

**テストファイル**: `app/src/test/java/com/den4dr/share2Obsidian/ui/template/TemplateEditViewModelTest.kt`

### テストケース2: save() が body を含む Template を保存 🔵

**信頼性**: 🔵 *TC-051-04 より*

**Given**: `body="## 記事\n{{content}}\n\n## メモ\n"`
**When**: `save()`
**Then**: `templateRepository.saveTemplate` に渡る Template の body が一致し、vault/folder を持たない

### テストケース3: body 空でも保存できる 🔵

**信頼性**: 🔵 *TC-051-05 より*

**Given**: `body=""`
**When**: `save()`
**Then**: バリデーションエラーなく保存される

---

## 統合テスト要件

### 統合テスト1: 保存→再読込で body 一致 🔵

**信頼性**: 🔵 *TC-051-04 より*

**テスト内容**: body を保存後、DB から取得して同じ body 値が得られる（Repository と結合）
**期待結果**: 保存前後で body が一致

---

## UI/UX要件

該当なし（ViewModel/State 層。UI は TASK-0051）。

---

## 実装手順

### TDDタスクの場合

1. `/tsumiki:tdd-requirements TASK-0050` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- 既存 `TemplateEditViewModelTest` の vault/folder 関連テストは削除し、body 関連テストへ置換する
- body のバリデーションは行わない（任意テキスト、空欄許容）

---

## 信頼性レベルサマリー

### 項目別信頼性

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 4 | 0 | 0 | 4 |
| 単体テスト | 3 | 0 | 0 | 3 |
| 統合テスト | 1 | 0 | 0 | 1 |

### 全体評価

- **総項目数**: 8項目
- 🔵 **青信号**: 8項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質
