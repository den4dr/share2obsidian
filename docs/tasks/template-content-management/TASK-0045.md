# TASK-0045: TemplateEntity・AppDatabase Migration(1→2)・Repository マッピング修正

**タスクID**: TASK-0045
**タスクタイプ**: TDD
**推定工数**: 8時間
**フェーズ**: Phase 1 - ドメイン・永続化基盤
**信頼性レベル**: 🔵 *database-schema.kt・REQ-003, REQ-004, NFR-001 より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/template-content-management/requirements.md) - REQ-003, REQ-004, NFR-001
- **設計文書**: [📐 architecture.md](../../design/template-content-management/architecture.md)
- **DBスキーマ**: [🗄️ database-schema.kt](../../design/template-content-management/database-schema.kt)
- **データフロー**: [🔄 dataflow.md](../../design/template-content-management/dataflow.md) - フロー7
- **受け入れ基準**: [✅ acceptance-criteria.md](../../spec/template-content-management/acceptance-criteria.md) - TC-001-03, TC-001-04

## タスク概要

Room 永続化層を Template モデル変更に追従させる。`TemplateEntity` から vault/folder を削除し body を追加、`AppDatabase` のスキーマバージョンを 1→2 に上げて `MIGRATION_1_2` を実装・登録する。`TemplateRepositoryImpl` の `toDomain()`/`toEntity()` マッピングを修正する。既存データ（name・isDefault・fields）を失わないマイグレーションを保証する。

## 依存タスク

- **前提タスク**: [TASK-0043](TASK-0043.md)（room-testing 依存）, [TASK-0044](TASK-0044.md)（Template.body）
- **後続タスク**: [TASK-0053](TASK-0053.md)

## 完了条件

- [ ] `TemplateEntity` から vault/folder が削除され `body: String = ""` が追加されている
- [ ] `AppDatabase` の version が 2 に更新され `MIGRATION_1_2` が `addMigrations` で登録されている
- [ ] `fallbackToDestructiveMigration()` を使用していない
- [ ] `TemplateRepositoryImpl` の toDomain/toEntity が body を扱い vault/folder を扱わない
- [ ] MigrationTestHelper による v1→v2 マイグレーションテスト（androidTest）が成功する
- [ ] 既存の `TemplateRepositoryImplTest` が新仕様に更新され成功する

---

## 実装詳細

### 1. TemplateEntity の変更 🔵

**信頼性**: 🔵 *database-schema.kt TemplateEntity 定義より*

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/data/db/TemplateEntity.kt`

```kotlin
@Entity(tableName = "templates")
data class TemplateEntity(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val name: String,
    val body: String = "",   // REQ-003 新規追加
    val isDefault: Boolean,
    // vault, folder を削除 (REQ-003)
)
```

### 2. AppDatabase バージョンアップと Migration 🔵

**信頼性**: 🔵 *database-schema.kt・architecture.md Migration 設計より*

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/data/db/AppDatabase.kt`

```kotlin
@Database(
    entities = [TemplateEntity::class, TemplateFieldEntity::class],
    version = 2,            // 1 → 2
    exportSchema = true,
)
abstract class AppDatabase : RoomDatabase() {
    abstract fun templateDao(): TemplateDao

    companion object {
        val MIGRATION_1_2 = object : Migration(1, 2) {
            override fun migrate(db: SupportSQLiteDatabase) {
                db.execSQL("ALTER TABLE templates ADD COLUMN body TEXT NOT NULL DEFAULT ''")
                db.execSQL("ALTER TABLE templates DROP COLUMN vault")
                db.execSQL("ALTER TABLE templates DROP COLUMN folder")
            }
        }
        // databaseBuilder に .addMigrations(MIGRATION_1_2) を追加
    }
}
```

> **注**: 既存の `AppDatabase` / DB 構築箇所（`DatabaseModule` または companion）に合わせて Migration 登録を実装する。`fallbackToDestructiveMigration()` は使用しない（NFR-001）。

### 3. TemplateRepositoryImpl マッピング修正 🔵

**信頼性**: 🔵 *database-schema.kt toDomain/toEntity 定義より*

**実装ファイル**: `app/src/main/java/com/den4dr/share2Obsidian/data/repository/TemplateRepositoryImpl.kt`

```kotlin
private fun TemplateWithFields.toDomain(): Template = Template(
    id = template.id,
    name = template.name,
    body = template.body,        // 新規
    isDefault = template.isDefault,
    fields = fields.map { /* 既存 */ },
)

private fun Template.toEntity(): TemplateEntity = TemplateEntity(
    id = id,
    name = name,
    body = body,                 // 新規
    isDefault = isDefault,
)
```

### 4. SQLite DROP COLUMN の前提確認 🔵

**信頼性**: 🔵 *architecture.md 技術的制約より*

- `ALTER TABLE ... DROP COLUMN` は SQLite 3.35+ が必要。minSdk 33（Android 13 = SQLite 3.39+）のため使用可能。

---

## 単体テスト要件

### テストケース1: toDomain/toEntity の body 往復 🔵

**信頼性**: 🔵 *TC-001-04・既存 TemplateRepositoryImplTest より*

**Given**: body を持つ Template/Entity
**When**: toEntity → toDomain で往復変換する
**Then**: body が保持され、vault/folder への参照が存在しない

**テストファイル**: `app/src/test/java/com/den4dr/share2Obsidian/data/repository/TemplateRepositoryImplTest.kt`

---

## 統合テスト要件

### 統合テスト1: Room Migration(1→2) 既存データ保護 🔵

**信頼性**: 🔵 *acceptance-criteria.md TC-001-03・dataflow.md フロー7・Room MigrationTestHelper 公式パターンより*

**テスト内容**: MigrationTestHelper で v1 スキーマの DB を作成し、`vault="my_vault", folder="notes", name="テンプレA", isDefault=1` のレコードを投入。`MIGRATION_1_2` を適用する。
**期待結果**: マイグレーション後も `name="テンプレA", isDefault=1` が保持され、`body=""` が追加されている。vault/folder カラムは存在しない。

**テストファイル**: `app/src/androidTest/java/com/den4dr/share2Obsidian/data/db/AppDatabaseMigrationTest.kt`（新規・androidTest）

```kotlin
@RunWith(AndroidJUnit4::class)
class AppDatabaseMigrationTest {
    @get:Rule
    val helper = MigrationTestHelper(
        InstrumentationRegistry.getInstrumentation(),
        AppDatabase::class.java,
    )
    // createDatabase(TEST_DB, 1) で v1 データ投入 → runMigrationsAndValidate(TEST_DB, 2, true, MIGRATION_1_2)
}
```

> v1 スキーマ JSON が `app/schemas` に存在することが前提（exportSchema=true により出力済みのはず）。なければ v1 を再現する初期スキーマを用意する。

---

## UI/UX要件

該当なし（永続化層）。

---

## 実装手順

### TDDタスクの場合

1. `/tsumiki:tdd-requirements TASK-0045` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- Migration テストは計器テスト（androidTest）であり、デバイス/エミュレータが必要
- `app/schemas` に v1 のスキーマ JSON が必要。存在を確認し、なければ初回の v1 スキーマを用意する
- DAO（`TemplateDao`）・`TemplateWithFields` は変更不要（body は Entity 側で吸収）

---

## 信頼性レベルサマリー

### 項目別信頼性

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 4 | 0 | 0 | 4 |
| 単体テスト | 1 | 0 | 0 | 1 |
| 統合テスト | 1 | 0 | 0 | 1 |

### 全体評価

- **総項目数**: 6項目
- 🔵 **青信号**: 6項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質
